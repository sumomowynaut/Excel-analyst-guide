# 速查卡

一页纸放最常用的东西。三版教程通用，建议收藏这一页。

## 函数速查

| 分类 | 函数 | 一句话 |
| --- | --- | --- |
| 统计 | `SUM` `AVERAGE` `MAX` `MIN` | 求和 / 平均 / 最大 / 最小 |
| 计数 | `COUNT` `COUNTA` `COUNTIF` `COUNTIFS` | 数数字 / 数非空 / 单条件 / 多条件 |
| 条件求和 | `SUMIF` `SUMIFS` `SUMPRODUCT` | 单条件 / 多条件 / 数组式多条件 |
| 逻辑 | `IF` `AND` `OR` `IFERROR` | 判断 / 且 / 或 / 容错 |
| 查找 | `VLOOKUP` `XLOOKUP` | 老版 / 新版（推荐） |
| 文本 | `LEFT` `RIGHT` `MID` `LEN` `TRIM` `TEXT` `&` | 截取、清洗、格式化、拼接 |
| 排名分布 | `RANK.EQ` `LARGE` `SMALL` `MEDIAN` `PERCENTILE.INC` `STDEV.S` | 名次、分位、波动 |
| 可见行求和 | `SUBTOTAL(109,…)` | **只算看得见的行**（筛选后别用 SUM） |
| 动态数组 | `UNIQUE` `SORT` `FILTER` | 365 专属，一个公式出一片 |
| 日期 | `TODAY` `DATEDIF` `EDATE` `EOMONTH` `MONTH` | 今天 / 间隔 / 加减月 / 月末 / 取月份 |
| DAX | `SUM` `DIVIDE` `CALCULATE` `COUNTROWS` | Power Pivot 四件套 |

## 快捷键

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl+T` | 转智能表（**拿到数据第一件事**） |
| `Ctrl+Shift+L` | 加 / 去筛选 |
| `Ctrl+;` | 输入当天日期 |
| `Ctrl+D` / `Ctrl+R` | 向下 / 向右填充 |
| `Ctrl+Shift+↓` | 选到列底 |
| `F4` | 切换绝对 / 相对引用 |
| `Ctrl+~` | 显示所有公式 |
| `Ctrl+E` | 闪电填充（给个样例自动识别规律） |
| `Ctrl+Alt+V` | 选择性粘贴（转数值、转置） |
| `Ctrl+G` → 定位条件 | 批量选中空值 / 公式 / 可见单元格 |

## 报错速查

| 报错 | 含义 | 怎么修 |
| --- | --- | --- |
| `#N/A` | 找不到 | 查空格（`TRIM`）、文本型数字、查找值是否在第一列 |
| `#VALUE!` | 类型不对 | 分列转成数字；检查是否混入全角符号 |
| `#REF!` | 引用失效 | `Ctrl+Z` 撤销，或重设公式区域 |
| `#DIV/0!` | 除以 0 | `IFERROR` 或 `DIVIDE` 兜底 |
| `#NAME?` | 函数名不认识 | 检查拼写和符号全半角 |
| `#####` | 列宽不够（不是错误） | 拉宽列 |

## 数据规范检查清单

拿到一份数据，先过这五关再开始分析：

- [ ] 表头只有**一行**，且不重名
- [ ] 数据区**没有合并单元格**
- [ ] **没有空行空列**切断数据区
- [ ] 一列只放**一种**数据（金额列里没有"待定""约1万")
- [ ] 已按 `Ctrl+T` 转成智能表

> 🔴 这五条不满足，透视表、Power Query、Power Pivot 要么报错，要么**静悄悄地算错**。

## 三条容易记反的规则

1. **`SUMIF` 条件区域在前，`SUMIFS` 求和区域在前** —— 带 S 的多条件，参数顺序反过来
2. **筛选后求和用 `SUBTOTAL(109,…)`，不是 `SUM`** —— SUM 会把隐藏行也算进去
3. **比率指标用「总和 ÷ 总和」，不是平均值** —— 客单价、转化率、毛利率都一样
