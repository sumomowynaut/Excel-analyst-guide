# Excel 从入门到精通（v1.0）：从打开软件到会用功能

> 🟡 **这一版的定位**：**轻量入门版**。9 章，覆盖日常表格工作 80% 会用到的东西，目标是**一天学完就能上手**，不追求全面。
> 想系统学请读 [v2.0（从会用功能到会做表）](Excel%E4%BB%8E%E5%85%A5%E9%97%A8%E5%88%B0%E7%B2%BE%E9%80%9A_v2.0_%E4%BB%8E%E4%BC%9A%E7%94%A8%E5%8A%9F%E8%83%BD%E5%88%B0%E4%BC%9A%E5%81%9A%E8%A1%A8.md)；想用 Excel 做业务分析请读 [v3.0（从会做表到会分析）](Excel%E4%BB%8E%E5%85%A5%E9%97%A8%E5%88%B0%E7%B2%BE%E9%80%9A_v3.0_%E4%BB%8E%E4%BC%9A%E5%81%9A%E8%A1%A8%E5%88%B0%E4%BC%9A%E5%88%86%E6%9E%90.md)。

> 🟡 **怎么用**
> 1. 练习数据都在**表格**里：全选表格 → 复制 → 粘到 Excel 的 **A1** → 自动分列。也可以直接下载配套的 `practice-data.xlsx`（用里面的 `10行数据` 工作表）
> 2. 答案都在 `✅ 参考答案` 标题下面，**先自己做完，再点开核对**
>
> 🔴 **最重要的一条**：**练习请用 Excel 桌面版（2021 / 365 / 2024）**。本版不讲 Power Query / Power Pivot，但飞书表格和 WPS 对部分函数（如 `XLOOKUP`）支持有限，会让你以为是学错了。

> 🟡 **版本说明**：基于 Excel 2021 / 365 / 2024。Mac 用户把 `Ctrl` 换成 `Cmd`。

---

## 第 1 章 先认识 Excel

> 🟡 **本章场景**：你第一次打开一张数据表，得先搞明白它的"操作语言"。
> 🟡 **学完你能交付**：一个建好三个工作表、录好一份规范数据的工作簿。

### 1.1 本质：单元格 + 关联计算

Excel 是一张由**单元格**组成的二维表，用"列标（字母）+ 行号（数字）"定位，比如 `A1`、`C2`。

它和"在 Word 里画表格"的根本区别是：**数据之间可以关联计算**——改一个数字，所有引用它的公式自动更新。

### 1.2 界面四块

- **功能区**：顶部操作区。新手先搞定「开始」「数据」「公式」三块，够应付八成工作。
- **工作表区**：行用数字、列用字母，交叉点是单元格。
- **状态栏**（右下角）：选中一片数字，直接显示计数和求和。**右键状态栏**可勾选平均值、最大值——快速看统计结果，不用写公式。
- **名称框 / 编辑栏**：显示当前单元格地址和内容。

### 1.3 先记住这 5 个快捷键

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl+S` | 保存（**养成每 5 分钟按一次的习惯**） |
| `Ctrl+T` | 转成智能表（拿到数据第一件事） |
| `Ctrl+Shift+L` | 加 / 去筛选 |
| `F4` | 切换绝对 / 相对引用 |
| `Ctrl+Z` | 撤销 |

### 1.4 日期的本质是数字

Excel 里数字 `1` = 1900年1月1日，`2` = 1月2日，以此类推。

理解这点你就明白：为什么日期能做加减（算天数差）、为什么日期有时会显示成一串数字（单元格被设成了"常规"格式，改回"日期"即可）。

### 1.5 练一练

1. 新建工作簿，命名 `Excel练习-入门`
2. 建 3 个工作表，重命名：`数据录入`、`格式测试`、`备份`
3. 在 `数据录入` 表录入下面的数据 —— **表头占第 1 行，数据从第 2 行往下**（全篇统一，后面公式都按这个位置写）
4. 全选数据区 → `Ctrl+T` 转成智能表
5. 右键状态栏勾选"平均值""最大值"，选中成交金额列看统计结果

**练习数据**

| 成交日期 | 销售工号 | 产品 | 成交客户数 | 成交金额 | 业务组 |
| --- | --- | --- | --- | --- | --- |
| 2026/1/1 | 001 | 花呗 | 28 | 16800 | 广东-1组 |
| 2026/1/2 | 002 | 借呗 | 15 | 7500 | 广东-2组 |
| 2026/1/3 | 003 | 花呗 | 32 | 18500 | 广西-1组 |
| 2026/1/4 | 001 | 借呗 | 21 | 10800 | 广东-1组 |
| 2026/1/5 | 002 | 花呗 | 19 | 9800 | 广东-2组 |
| 2026/1/6 | 003 | 借呗 | 25 | 13200 | 广西-1组 |
| 2026/1/7 | 001 | 花呗 | 30 | 17500 | 广东-1组 |
| 2026/1/8 | 002 | 借呗 | 12 | 6300 | 广东-2组 |
| 2026/1/9 | 003 | 花呗 | 23 | 12600 | 广西-1组 |
| 2026/1/10 | 001 | 借呗 | 27 | 14200 | 广东-1组 |

> 🟡 **关于工号 `001`**：粘贴后可能显示成 `1`，不是错了，只是 Excel 当成了数字。想保持三位显示：选中该列 → 设置单元格格式 → 自定义 → 输入 `000`。

### ✅ 参考答案（做完再看）

- 表头在第 **1** 行，数据在第 **2–11** 行，共 10 条 × 6 列
- 状态栏看到的成交金额：平均 **12720**、最大 **18500**、计数 **10**
- 这份表是全篇起点，**建议另存一份备份**

---

## 第 2 章 录入与格式

> 🟡 **本章场景**：数据要录进去，还要让它看起来专业、重点自己跳出来。
> 🟡 **学完你能交付**：一张格式规范、重点高亮的明细表。

### 2.1 录入技巧

- **序列填充**：输入前两个值（如 `1`、`2`，或 `1/1`、`1/2`），选中后**拖拽右下角填充柄**自动生成序列。按住 `Ctrl` 拖拽则强制复制。
- **批量填充**：双击填充柄，快速向下复制到底。
- **数据验证（下拉框）**：`数据 → 数据验证 → 允许"序列" → 来源填"花呗,借呗"`（**英文逗号**）。这样只能选，杜绝错别字。

> 🔴 下拉框来源必须用**英文逗号**分隔。用中文逗号 `，` 会被当成一个整体选项。

### 2.2 数字格式

`开始 → 数字`：金额用「货币」，增长率用「百分比」，数量用「数值」并设好小数位。

> 🟡 这一步决定报表是否专业。金额不设货币格式、百分比不设百分比格式，是最常见的"看着不专业"的来源。

### 2.3 条件格式：让重点自己跳出来

`开始 → 条件格式`。选中成交金额列 → 突出显示单元格规则 → 大于 → 输入 `15000` → 红色填充。

进阶玩法还有**数据条、色阶、图标集**——数据条能在单元格里直接画出条形图，做业绩对比时比看数字快得多。

### 2.4 练一练

在 `格式测试` 表操作（可以复制上一章的数据过去）：

1. 成交金额列设为「货币」格式，0 位小数；成交客户数设为「数值」
2. 条件格式：金额 ≥ 15000 标红，≤ 8000 标蓝
3. 给"产品"列加下拉框（花呗 / 借呗）
4. 加完整边框，冻结首行
5. 按成交金额降序排列，看看第一行是多少

### ✅ 参考答案（做完再看）

- 条件格式两条规则：单元格值 `>=` 15000（红填充）、`<=` 8000（蓝填充）
- 降序后第一行应为 **18500**
- 下拉框来源填：`花呗,借呗`
- 冻结首行：选中第 2 行 → `视图 → 冻结窗格`

---

## 第 3 章 把数据整理干净

> 🟡 **本章场景**：数据录进来了，但顺序乱、有重复、想按条件挑着看。

### 3.1 一条铁律：一维表

**一行是一条记录，一列是一个字段，表头只有一行**。

| 能做 | 不能做 |
| --- | --- |
| 表头只有一行、不重名 | 表头两行、合并单元格 |
| 无空行空列切断数据区 | 中间隔一行写"以下为二月数据" |
| 一列只放一种数据 | 金额列里写"约1万" |

> 🔴 **合并单元格绝对不要用在数据区**——合并过的表无法排序筛选，透视表会直接失败。标题要合并就在数据表**外面**的空行做。

### 3.2 整理四件套

| 功能 | 入口 / 快捷键 | 要点 |
| --- | --- | --- |
| 查找替换 | `Ctrl+F` / `Ctrl+H` | 替换时勾选「匹配整个单元格」避免误替换 |
| 排序 | `数据 → 排序` | 可设多个条件（先按金额降序，再按日期升序） |
| 筛选 | `Ctrl+Shift+L` | 多个筛选条件是"同时满足" |
| 删除重复值 | `数据 → 删除重复值` | **会直接删行，操作前先备份** |

**Ctrl+T 智能表**：拿到任何数据第一件事。好处是数据区自动扩展、自动隔行配色、自带筛选。

> 🟢 **智能表还有一招**：转表后公式可以写 `=SUM(销售明细[成交金额])` 而不是 `=SUM(E2:E11)`——一眼看出在算什么；而且追加新行时范围自动跟上，**公式不用改**。

### 3.3 练一练

1. 复制 `数据录入` 的数据到 `格式测试`
2. 筛选出「产品 = 花呗」且「成交金额 ≥ 10000」的记录，数数有几条
3. 按业务组排序，把三个组排在一起
4. 冻结首行后滚动，确认表头不动

### ✅ 参考答案（做完再看）

- 筛选「花呗 + ≥10000」共 **4 条**（16800、18500、17500、12600）
- 业务组共 3 个：广东-1组、广东-2组、广西-1组
- 冻结首行后向下滚动，第 1 行表头保持可见

---

## 第 4 章 公式与基础函数

> 🟡 **本章场景**：要回答"总共多少""平均多少""最大是多少"。
> 🟡 **学完你能交付**：一个带统计汇总区的明细表。

### 4.1 公式的三条规则

1. **必须以 `=` 开头**。直接输入 `SUM(A1:A10)` 只是文本。
2. **运算符**：加 `+`、减 `-`、乘 `*`、除 `/`；比较用 `>` `<` `>=` `<=` `<>`（不等于）。
3. **函数结构**：`=函数名(参数1, 参数2, …)`，参数之间用**英文逗号**。

> 🔴 **全角符号是新手第一大坑**
> `≥` `≤` `≠` 是中文字符，Excel 不认识，写进公式会报错或被当成文本。必须写 `>=` `<=` `<>`。
> 中文逗号 `，`、中文引号 `“”`、中文括号 `（）` 同理，都要换成英文半角。

### 4.2 六个统计函数

| 函数 | 作用 | 注意点 |
| --- | --- | --- |
| `SUM(区域)` | 求和 | 文本、空白自动忽略 |
| `AVERAGE(区域)` | 平均 | 空白不计入分母 |
| `MAX` / `MIN` | 最大 / 最小 | 文本不参与 |
| `COUNT(区域)` | 计数 | **只数数字** |
| `COUNTA(区域)` | 计数 | 数**所有非空**，文本也算 |

### 4.3 引用类型：公式能不能复制的关键

| 类型 | 写法 | 复制时会怎样 |
| --- | --- | --- |
| 相对引用 | `A1` | 行号、列标都跟着变 |
| 绝对引用 | `$A$1` | 完全不动 |
| 混合引用 | `$A1` / `A$1` | 只锁列 / 只锁行 |

按 **F4** 在四种状态间循环切换。跨表引用：`=SUM(数据录入!E2:E11)`。

写好第一个公式后，**双击单元格右下角的填充柄**自动向下填到底。

### 4.4 练一练

在 `数据录入` 表继续做：

1. 在 A13 起建汇总区，算出：总成交额、平均成交额、最大单笔、最小单笔、成交笔数、客户数合计
2. 新增 H 列「客单价」，`=E2/D2`，保留 2 位小数，向下填充
3. 想一想：`AVERAGE(H2:H11)` 和 `SUM(E2:E11)/SUM(D2:D11)` 结果一样吗？

### ✅ 参考答案（做完再看）

```text
总成交额    =SUM(E2:E11)
平均成交额  =AVERAGE(E2:E11)
最大单笔    =MAX(E2:E11)
最小单笔    =MIN(E2:E11)
成交笔数    =COUNTA(A2:A11)
客户数合计  =SUM(D2:D11)
```

| 指标 | 结果 |
| --- | --- |
| 总成交额 | **127200** |
| 平均成交额 | **12720** |
| 最大单笔 | **18500** |
| 最小单笔 | **6300** |
| 成交笔数 | **10** |
| 客户数合计 | **232** |

- 客单价（`=E2/D2`，向下填充）：600.00、500.00、578.12、514.29、515.79、528.00、583.33、525.00、547.83、525.93
- **两个算法不一样**：`AVERAGE(H2:H11)` = **541.83**（每笔客单价的算术平均）；`SUM(E)/SUM(D)` = **548.28**（整体客单价）

> 🔴 **算比率类指标（客单价、转化率、毛利率），永远用「总和 ÷ 总和」，不要用平均值。**

---

## 第 5 章 三个最常用的函数

> 🟡 **本章场景**：不只问"总共多少"，还要问"花呗总共多少""超过 15000 的有几笔""这个工号是谁"。

### 5.1 IF：做判断

```text
=IF(条件, 条件成立时的值, 条件不成立时的值)
```

多档判断用嵌套，注意**从大到小**依次判断：

```text
=IF(E2>=15000,"大额",IF(E2>=10000,"中额","小额"))
```

多条件同时成立用 `AND`，任一成立用 `OR`：

```text
=IF(AND(C2="花呗",E2>=15000),"重点","普通")
```

### 5.2 SUMIF / COUNTIF：带条件统计

```text
=SUMIF(条件区域, 条件, [求和区域])
=COUNTIF(条件区域, 条件)
```

**条件的写法**

| 想表达 | 写法 | 说明 |
| --- | --- | --- |
| 等于文本 | `"花呗"` | 文本加英文双引号 |
| 大于数字 | `">=15000"` | **比较符和数字一起放进引号** |
| 引用单元格 | `">="&H1` | 比较符在引号里，单元格用 `&` 拼在外面 |

> 🟡 写成 `">=H1"` 的话，Excel 会把 `H1` 当成两个字符而不是单元格地址。

### 5.3 VLOOKUP：查表

```text
=VLOOKUP(查找值, 查找区域, 返回第几列, 匹配方式)
```

- 查找值必须在**查找区域的第一列**
- 匹配方式填 `0`（或 `FALSE`）是精确匹配；**填 1 或省略是模糊匹配，会给出错误结果**
- 公式几乎一定要用**绝对引用**锁定查找区域，否则向下复制时区域会跑偏

```text
=VLOOKUP(G1,$B$2:$F$11,4,0)
```

**VLOOKUP 的三个硬伤**

1. 只能从左往右查
2. 第 3 参数是写死的数字，中间插一列就全错
3. 找不到时返回 `#N/A`

> 🟢 新版 Excel（2021 / 365）有 **XLOOKUP**，查找列和返回列分开写、可以反向查、内置容错，能用就用它：
> `=XLOOKUP(G1,$B$2:$B$11,$C$2:$C$11,"未找到")`

### 5.4 练一练

1. 用 `SUMIF` 分别算花呗、借呗的成交总额
2. 用 `COUNTIF` 算：金额 ≥ 15000 有几笔；花呗有几笔
3. 用 `SUMIF` 算 001 号销售的成交总额
4. 新增 I 列「规模标签」：≥15000 记"大额"，≥10000 记"中额"，否则"小额"
5. 建工号汇总区（001/002/003），用 `SUMIF` + 绝对引用算各自成交额

### ✅ 参考答案（做完再看）

```text
花呗总额    =SUMIF(C2:C11,"花呗",E2:E11)
借呗总额    =SUMIF(C2:C11,"借呗",E2:E11)
大额笔数    =COUNTIF(E2:E11,">=15000")
花呗笔数    =COUNTIF(C2:C11,"花呗")
001 总额    =SUMIF(B2:B11,"001",E2:E11)
```

| 题目 | 结果 |
| --- | --- |
| 花呗总额 | **75200** |
| 借呗总额 | **52000** |
| 金额 ≥15000 的笔数 | **3**（合计 52800） |
| 花呗笔数 | **5** |
| 001 号销售总额 | **59300** |

- 规模标签：`=IF(E2>=15000,"大额",IF(E2>=10000,"中额","小额"))`
  结果：大额 **3** 笔、中额 **4** 笔、小额 **3** 笔
- 工号汇总区（工号在 K2:K4，公式在 L2 向下填充，**区域必须绝对引用**）：

```text
=SUMIF($B$2:$B$11,K2,$E$2:$E$11)
```

| 工号 | 成交额 |
| --- | --- |
| 001 | **59300** |
| 002 | **23600** |
| 003 | **44300** |

---

## 第 6 章 文本与日期处理

> 🟡 **本章场景**：工号丢了前导零、要从"广东-1组"里拆出省份、要算两个日期隔了多少天。

### 6.1 `&` 拼接

用 `&` 把文本拼起来，固定文本用英文双引号包住。

```text
=A1&"-"&B1
```

A1 是 `广东`、B1 是 `1组`，结果是 `广东-1组`。拼接单号、全名都靠它。

### 6.2 截取三件套

| 函数 | 作用 | 例子 |
| --- | --- | --- |
| `LEFT(文本,N)` | 从左边取 N 个字符 | `=LEFT(F2,2)` → `广东` |
| `RIGHT(文本,N)` | 从右边取 N 个字符 | `=RIGHT(F2,2)` → `1组` |
| `MID(文本,起点,N)` | 从中间取 | `=MID(F2,4,1)` → `1` |
| `LEN(文本)` | 字符数 | 查位数、查多余空格 |

### 6.3 清洗与格式化

| 函数 | 作用 |
| --- | --- |
| `TRIM(文本)` | 去掉首尾空格 —— **VLOOKUP 匹配不上的头号嫌疑** |
| `TEXT(数值,"格式")` | 按格式转成文本，如 `=TEXT(B2,"000")` → `001` |

> 🟡 `TEXT(B2,"000")` 是保住工号前导零最稳的做法。注意结果是**文本**，不能再参与数值计算。

### 6.4 日期函数

| 函数 | 作用 |
| --- | --- |
| `TODAY()` | 今天 |
| `MONTH(日期)` | 取月份，用于按月汇总 |
| `DATEDIF(开始,结束,"单位")` | 算间隔，`"D"` 天、`"M"` 月、`"Y"` 年 |
| `EDATE(日期,N)` | 加减 N 个月 |
| `EOMONTH(日期,0)` | 当月最后一天 |

### 6.5 练一练

1. 新增 G 列「业务标识」，用 `&` 把工号和产品拼成 `001-花呗`
2. 用 `LEFT` 从"业务组"里提取省份
3. 用 `TEXT` 把工号统一成三位
4. 用 `MONTH` 从成交日期提取月份
5. 用 `DATEDIF` 算第一笔和最后一笔成交隔了多少天

### ✅ 参考答案（做完再看）

```text
业务标识    =B2&"-"&C2
省份        =LEFT(F2,2)
三位工号    =TEXT(B2,"000")
月份        =MONTH(A2)
间隔天数    =DATEDIF(A2,A11,"D")
```

- 业务标识第一行：`001-花呗`
- 省份提取结果：广东、广东、广西、广东、广东、广西、广东、广东、广西、广东
- 月份全是 **1**（数据都在 1 月）
- 第一笔（1/1）到最后一笔（1/10）间隔 **9** 天

---

## 第 7 章 数据透视表

> 🟡 **本章场景**：老板要"按业务组、按产品看成交额"。不用写公式，拖几下就出来。
> 🟡 **学完你能交付**：一张可交互的多维汇总表。

### 7.1 怎么建

选中数据区（或智能表内任意单元格）→ `插入 → 数据透视表` → 确定。

> 🟢 **前提**：数据源必须是一维表——表头唯一、无合并单元格、无空行。不满足就先回去修数据。

### 7.2 四个区域

| 区域 | 放什么 | 效果 |
| --- | --- | --- |
| 行 | 分类字段（业务组、产品） | 纵向展开 |
| 列 | 分类字段（月份） | 横向展开 |
| 值 | 要算的字段（成交金额） | 默认求和，可改计数/平均 |
| 筛选 | 全局过滤字段 | 整表生效 |

**值字段设置**：点值区域的下拉 → 值字段设置 → 可切换**求和 / 计数 / 平均值 / 最大值**，数字格式也在这里改。

> 🟡 文本字段拖进去默认是"计数"，数字字段默认是"求和"。

### 7.3 直接显示占比

值字段设置 → **值显示方式** → 「占总和的百分比」，透视表会直接算出每行占总计的百分比，不用你自己写除法。

### 7.4 切片器：把筛选做成按钮

`透视表分析 → 插入切片器`（选业务组、产品）。点一下就换一版数据，做汇报时比下拉筛选好用得多。

### 7.5 练一练

1. 建透视表：行 = 业务组，值 = 成交金额
2. 再加一个「占总和的百分比」的值字段
3. 改成：行 = 业务组，列 = 产品
4. 插入切片器（业务组），点一下看联动

### ✅ 参考答案（做完再看）

**按业务组**

| 业务组 | 成交额 | 占比 |
| --- | --- | --- |
| 广东-1组 | **59300** | 46.6% |
| 广西-1组 | **44300** | 34.8% |
| 广东-2组 | **23600** | 18.6% |
| 总计 | **127200** | 100% |

**业务组 × 产品**

| 业务组 | 花呗 | 借呗 | 合计 |
| --- | --- | --- | --- |
| 广东-1组 | 34300 | 25000 | 59300 |
| 广东-2组 | 9800 | 13800 | 23600 |
| 广西-1组 | 31100 | 13200 | 44300 |
| 合计 | 75200 | 52000 | 127200 |

---

## 第 8 章 图表：用哪种图

> 🟡 **本章场景**：数字摆出来了，但老板看不进去。

### 8.1 选图的原则

| 你想表达 | 用什么 |
| --- | --- |
| 几个类别比大小 | **柱状图**（横向比较最准） |
| 随时间的变化 | **折线图** |
| 部分占整体的比例 | 饼图 / 环形图（**类别不超过 5 个才用**） |
| 两个指标的关系 | **散点图**（看相关性） |
| 分布情况 | 直方图 |

> 🔴 **三个最常见的错**：① 类别超过 7 个还用饼图（没人看得清）；② 用 3D 效果（会扭曲比例）；③ 一个图里塞五种颜色。

### 8.2 做一张图的步骤

1. 选中数据区（含表头）
2. `插入 → 图表` → 选类型
3. 点图表 → 右上角「图表元素」按钮，加标题、数据标签
4. 双击坐标轴可设刻度；右键数据系列可改颜色

> 🟡 **做汇报用的图，先删掉所有不必要的东西**：网格线、图例（能直接标数据就别放图例）、多余的边框。留白越多，重点越突出。

### 8.3 练一练

1. 用第 7 章的透视表数据，做一张「各业务组成交额」柱状图
2. 加标题和数据标签
3. 再做一张「花呗 vs 借呗」占比图
4. 把两张图的网格线删掉，对比前后观感

### ✅ 参考答案（做完再看）

- 柱状图：广东-1组（59300）最高，广东-2组（23600）最低，差距约 2.5 倍
- 占比图：花呗 **59.1%**、借呗 **40.9%**——两个类别用饼图合适，超过 5 个就该换柱状图
- 删掉网格线后图表会明显清爽，这是最快见效的一步

---

## 第 9 章 报错了怎么办

> 🟡 **本章场景**：公式写出来是一串 `#N/A`，不知道哪里错了。

### 9.1 五大报错

| 报错 | 含义 | 常见原因 | 怎么修 |
| --- | --- | --- | --- |
| `#N/A` | 找不到 | VLOOKUP 没匹配上 | 查**空格**（用 `TRIM`）、文本型数字、查找值是否在第一列 |
| `#VALUE!` | 类型不对 | 把文本当数字算 | 分列转成数字；检查是否混入全角符号 |
| `#REF!` | 引用失效 | 删了被引用的行/列 | `Ctrl+Z` 撤销，或重设公式区域 |
| `#DIV/0!` | 除以 0 | 分母为空或 0 | 用 `=IFERROR(原公式,0)` 兜底 |
| `#NAME?` | 函数名不认识 | 拼错、用了中文引号 | 检查拼写和符号全半角 |

> 🟡 `#####` 不是错误，只是**列宽不够**，拉宽即可。

### 9.2 排查三步

1. 选中出错单元格 → `公式 → 公式求值`，一步步看中间结果
2. `Ctrl+~` 显示所有公式，一眼看出哪几行的区域跑偏了
3. 检查**全半角**：把公式里的符号全换成英文的，一半的错就没了

---

## 附录 快捷键速查

| 分类 | 快捷键 | 作用 |
| --- | --- | --- |
| 文件 | `Ctrl+S` / `F12` | 保存 / 另存为 |
| 编辑 | `Ctrl+C` `Ctrl+V` `Ctrl+Z` | 复制 / 粘贴 / 撤销 |
| 填充 | `Ctrl+D` / `Ctrl+R` | 向下 / 向右填充 |
| 选中 | `Ctrl+Shift+↓` | 选到列底 |
| 表格 | `Ctrl+T` | 转智能表（**最重要**） |
| 筛选 | `Ctrl+Shift+L` | 加 / 去筛选 |
| 查找 | `Ctrl+F` / `Ctrl+H` | 查找 / 替换 |
| 公式 | `F4` | 切换绝对 / 相对引用 |
| 公式 | `Ctrl+~` | 显示所有公式 |
| 日期 | `Ctrl+;` | 输入当天日期 |
| 快速填充 | `Ctrl+E` | 闪电填充：给一个样例自动识别规律填满整列 |
| 粘贴 | `Ctrl+Alt+V` | 选择性粘贴（转数值、转置） |

---

> 🟡 **到这里**：你已经能处理日常 80% 的表格工作了——录数据、整理、算汇总、做透视表、出图、排错。
>
> **下一步**：
> - 想系统补齐（规范数据、Power Query 自动化、Power Pivot 数据模型、DAX）→ **[v2.0 从会用功能到会做表](Excel%E4%BB%8E%E5%85%A5%E9%97%A8%E5%88%B0%E7%B2%BE%E9%80%9A_v2.0_%E4%BB%8E%E4%BC%9A%E7%94%A8%E5%8A%9F%E8%83%BD%E5%88%B0%E4%BC%9A%E5%81%9A%E8%A1%A8.md)**
> - 想用 Excel 回答业务问题（为什么跌了、谁拖的、该做什么）→ **[v3.0 从会做表到会分析](Excel%E4%BB%8E%E5%85%A5%E9%97%A8%E5%88%B0%E7%B2%BE%E9%80%9A_v3.0_%E4%BB%8E%E4%BC%9A%E5%81%9A%E8%A1%A8%E5%88%B0%E4%BC%9A%E5%88%86%E6%9E%90.md)**
