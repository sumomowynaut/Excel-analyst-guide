# Excel 从入门到精通（v2.0）：从会用功能到会做表

> **版本说明**：v2.0 = 修正版。修掉了 v1.0 的硬伤（全角符号、参考答案偏差、章节顺序），补齐了缺失的基础模块（数据规范、排错手册、XLOOKUP 等），并改为**一个文档自包含**的形态（数据表可直接在文档内复制）。
> 与 v3.0 的区别：v2.0 仍按 **Excel 功能模块**组织（适合"学会用 Excel"）；v3.0 改为按**分析师工作流**组织，并新增分析方法和 AI 章节（适合"学会做分析"）。

> 🟡 **怎么用**
> 1. 练习数据都在**表格**里：全选表格 → 复制 → 粘到 Excel 的 **A1** → 自动分列。也可以直接下载配套的 `practice-data.xlsx`
> 2. 答案都在 `✅ 参考答案` 标题下面，**先自己做完，再点开核对**
>
> 🔴 **最重要的一条**：**练习请用 Excel 桌面版（2021 / 365 / 2024）**。Power Query、Power Pivot、动态数组这些在飞书表格和 WPS 里不支持，在那儿练会以为是自己学错了。

> 🟡 **版本说明**
> 本教程基于 **Excel 2021 / 365 / 2024**。WPS 用户请注意：**第 7、8 章（Power Query / Power Pivot）在 WPS 中不支持或支持有限**，建议用 Excel 桌面版学习这两章，其余章节不受影响。Mac 用户把 `Ctrl` 换成 `Cmd`。

---

## 第 0 章 开工前：先把数据做成"能用的样子"

> 🔵 **为什么这一章最重要**：后面所有高级功能——透视表、Power Query、Power Pivot——都有一个共同前提：**数据源必须规范**。数据不规范，这些功能要么报错，要么算出莫名其妙的结果。这一章是地基，别跳过。

### 0.1 一维表：唯一正确的数据形态

一份"能用"的数据表长这样：**一行就是一条记录，一列就是一个字段，表头只有一行**。

**✅ 正确（一维表）**

| 成交日期 | 销售工号 | 产品 | 成交客户数 | 成交金额 | 业务组 |
| --- | --- | --- | --- | --- | --- |
| 2026/1/1 | 001 | 花呗 | 28 | 16800 | 广东-1组 |
| 2026/1/2 | 002 | 借呗 | 15 | 7500 | 广东-2组 |
| 2026/1/3 | 003 | 花呗 | 32 | 18500 | 广西-1组 |

**❌ 错误（二维交叉表，最常见的毛病）**

| 销售工号 | 1月 | 2月 | 3月 |
| --- | --- | --- | --- |
| 001 | 12500 | 16800 | 11800 |
| 002 | 9800 | 13200 | 14300 |

第二种看着"更适合人看"，但 Excel 没法分析它：你想加一列"产品"就没地方加，做透视表也出不来。

> 🔴 **四条铁律**
> 1. **表头只有一行**，且不能重名
> 2. **不要合并单元格**（做标题用的合并放在数据表外面）
> 3. **不要有空行、空列**切断数据区
> 4. **一列只放一种数据**（别在金额列里写"约1万"这种）

### 0.2 Ctrl+T：让普通区域变成"智能表"

选中数据区（含表头）→ 按 **Ctrl+T** → 确认"包含表头" → 确定。

这一步带来的好处，够你受用整份教程：

- 数据区**自动扩展**，新增行会被自动纳入公式和图表
- 自动**隔行配色**、自动带筛选按钮
- 做透视表 / Power Query 时，它是**正当的数据源**（普通区域容易错乱）
- 公式里可以用**列名**代替 `A1:B10` 这种坐标（下面单独讲）

#### 用列名写公式（结构化引用）

转成智能表之后，这张表就有了名字，公式里可以直接写**列名**，不用再数"第几列、到第几行"：

| 写法 | 例子 | 感受 |
| --- | --- | --- |
| 单元格坐标 | `=SUM(E2:E11)` | 看不出 E 列是什么，也不知道为什么到 11 行 |
| 结构化引用 | `=SUM(销售明细[成交金额])` | 一眼就知道在算什么 |

**先给表起个名字**：点「表设计」→ 左上角「表名称」，把默认的「表1」改成 `销售明细`。

然后就能这么写（函数第 3 章才正式学，这里先看写法）：

```text
=SUM(销售明细[成交金额])
=COUNTIF(销售明细[成交金额],">=15000")
```

在表内新增一列时，用 `[@列名]` 表示"当前行的这一列"：

```text
=[@成交金额]/[@成交客户数]
```

回车后会**自动填满整列**，不用拖填充柄。这种列叫**计算列**。

| 写法 | 含义 | 用在哪 |
| --- | --- | --- |
| `表名[列名]` | 这一整列（不含表头） | 表内、表外都能用 |
| `[@列名]` | 当前行的这一列 | **只在表内** |
| `表名[[#全部],[列名]]` | 含表头的整列 | 很少用 |

> 🟢 **真正的价值不是好看，是自动扩展**：在第 12 行追加一条新数据，`销售明细[成交金额]` 会自动把它算进去，公式一个字都不用改；而 `SUM(E2:E11)` 不会，你得手动改成 `E2:E12`。这是长期维护报表最容易出错的地方。

> 🔴 **一个坑**：`[@列名]` 只能在**表内**用，复制到表外会报错。表外引用请用 `表名[列名]`。另外表名不能有空格（中文可以，空格不行）。

> 🟢 **最佳实践**：拿到任何一份数据，**第一件事就是 Ctrl+T**。这是整份教程里投入产出比最高的一个快捷键。

### 0.3 三种典型"脏数据"的快速处理

| 症状 | 表现 | 怎么修 |
| --- | --- | --- |
| 文本型数字 | 单元格左上角有绿色三角，SUM 算不进去 | 选中列 → 数据 → 分列 → 完成 |
| 多余空格 | 看着一样但 VLOOKUP 匹配不上 | 用 `=TRIM()` 清洗 |
| 日期是文本 | 无法按月分组，排序错乱 | 用 `=DATEVALUE()` 转换，或分列时选"日期" |

---

## 第 1 章 基础认知

> 🟡 **本章场景**：你刚接手一张原始数据表，要先搞明白 Excel 的"操作语言"。
> 🟡 **学完你能交付**：一个建好三个工作表、录好一份规范数据的练习工作簿。

### 1.1 核心逻辑原理

Excel 的本质是一张由**单元格**组成的二维表，通过"列标（字母）+ 行号（数字）"定位，比如 `A1`、`C2`。

它的核心价值在于：数据之间可以**关联计算**——改一个数字，所有引用它的公式自动更新。这是它和"在 Word 里画表格"的根本区别。

### 1.2 界面组成

- **功能区**：核心操作区。新手先搞定「开始」「数据」「公式」三块就够应付八成工作。
- **工作表区**：行用数字、列用字母，交叉点是单元格。
- **状态栏**（右下角）：选中一片数字，它会直接显示计数、求和。**右键状态栏**可以勾选平均值、最大值等，用来快速看统计结果，不用写公式。
- **名称框 / 编辑栏**：显示当前单元格地址和内容。

### 1.3 核心基础操作

**快捷键（必须记住的四个）**

| 操作 | 快捷键 |
| --- | --- |
| 新建 | `Ctrl+N` |
| 保存 | `Ctrl+S` |
| 另存为 | `F12` |
| 智能表 | `Ctrl+T` |

**单元格操作**

- 定位：列标 + 行号，如 `A1`；连续区域写左上角:右下角，如 `A2:H3`
- 录入：单击输入，`Enter` 确认；`F2` 或双击进入修改
- 清除内容：`Delete`（注意：这是清内容，不是删单元格）
- 选中：拖拽选连续区，`Ctrl+单击` 选不连续，`Shift+单击` 选连续

**行列操作**：右键行号 / 列标可插入、删除；拖拽边界调整行高列宽，**双击边界自动适配内容**。

**格式基础**

- 对齐：文本常用居中，数字建议右对齐（位数对齐才好比较）
- 格式刷：`开始 → 格式刷`，**双击格式刷可以连续刷多次**，`Esc` 退出
- 网格线：`视图 → 显示`，取消勾选可隐藏（做汇报表时更清爽）

> 🔴 **关于"合并单元格"**：它能做出好看的标题，但**绝对不要用在数据区**——合并过的表格无法排序、筛选，透视表也会直接失败。标题要合并，就在数据表**外面**的空行上做。

### 1.4 日期的本质是数字

Excel 里，数字 `1` = 1900年1月1日，`2` = 1月2日，以此类推。

理解这一点，你就能明白为什么日期可以做加减（算天数差）、为什么日期显示成一串数字（单元格被设成了"常规"格式，把它改回"日期"即可）。

### 1.5 练一练

1. 新建工作簿，命名 `Excel练习-基础认知`
2. 建 3 个工作表，重命名：`数据录入`、`格式测试`、`备份`
3. 在 `数据录入` 表录入下面的数据 —— **表头占第 1 行，数据从第 2 行往下**（全篇统一这个位置，后面公式都按它来写）
4. 全选数据区 → `Ctrl+T` 转成智能表
5. 调整列宽自动适配、行高设为 20；表头加粗、加填充色
6. 用格式刷把表头样式刷到 `格式测试` 表
7. 右键状态栏勾选"平均值""最大值"，选中成交金额列，看看统计结果

**数据（点右上角复制，粘到 A1 会自动分列）**

| 成交日期 | 销售工号 | 产品 | 成交客户数 | 成交金额 | 业务组 |
| --- | --- | --- | --- | --- | --- |
| 2026/1/1 | 001 | 花呗 | 28 | 16800 | 广东-1组 |
| 2026/1/2 | 002 | 借呗 | 15 | 7500 | 广东-2组 |
| 2026/1/3 | 003 | 花呗 | 32 | 18500 | 广西-1组 |
| 2026/1/4 | 001 | 借呗 | 21 | 10800 | 广东-1组 |
| 2026/1/5 | 002 | 花呗 | 19 | 9800 | 广东-2组 |
| 2026/1/6 | 003 | 借呗 | 25 | 13200 | 广西-1组 |
| 2026/1/7 | 001 | 花呗 | 30 | 17500 | 广东-1组 |
| 2026/1/8 | 002 | 借呗 | 12 | 6300 | 广东-2组 |
| 2026/1/9 | 003 | 花呗 | 23 | 12600 | 广西-1组 |
| 2026/1/10 | 001 | 借呗 | 27 | 14200 | 广东-1组 |

> 🟡 **关于工号 `001`**：粘贴后可能显示成 `1`。这不是错了，只是 Excel 把它当成了数字。想保持三位显示，选中该列 → 右键设置单元格格式 → 自定义 → 输入 `000`。后续章节我们按文本处理它。

### ✅ 参考答案（做完再看）

- 表头在第 **1** 行，数据在第 **2–11** 行，共 10 条 × 6 列
- 状态栏看到的成交金额：平均 **12720**、最大 **18500**、计数 **10**
- 这份表是全篇的起点，**建议另存一份备份**，后面章节都基于它

---

## 第 2 章 常用功能

> 🟡 **本章场景**：数据录进来了，但格式乱、有重复、想按条件挑着看。
> 🟡 **学完你能交付**：一张格式规范、可筛选、可排序的干净明细表。

### 2.1 格式设置

- **数字格式**（`开始 → 数字`）：金额用「货币」，增长率用「百分比」，数量用「数值」并设好小数位。**这一步决定报表是否专业**。
- **边框 / 填充**：数据区加边框（否则打印出来没线），标题行加填充色。
- **条件格式**（`开始 → 条件格式`）：让满足条件的数据自动变样式，是"让数据自己说话"的核心工具。

**条件格式实操**：选中成交金额列 → 条件格式 → 突出显示单元格规则 → 大于 → 输入 `15000` → 设为红色填充。

> 🟡 **进阶玩法**：条件格式里还有「数据条」「色阶」「图标集」。数据条能在单元格里直接画出条形图，做业绩对比时一眼看出高低，比看数字快得多。

### 2.2 数据录入技巧

- **序列填充**：输入前两个值（如 `1`、`2` 或 `1/1`、`1/2`），选中后**拖拽右下角的填充柄**自动生成序列。按住 `Ctrl` 拖拽则强制复制。
- **批量填充相同内容**：双击填充柄可快速向下复制。
- **数据验证（下拉框）**：`数据 → 数据验证 → 允许"序列" → 来源填"花呗,借呗"`（**英文逗号分隔**）。这样录入时只能选，杜绝错别字。

### 2.3 数据整理四件套

| 功能 | 入口 / 快捷键 | 要点 |
| --- | --- | --- |
| 查找替换 | `Ctrl+F` / `Ctrl+H` | 替换时勾选「匹配整个单元格」可避免误替换 |
| 排序 | `数据 → 排序` | 可设多个条件（先按金额降序，再按日期升序） |
| 筛选 | `Ctrl+Shift+L` | 多个筛选条件是「同时满足」的关系 |
| 删除重复值 | `数据 → 删除重复值` | **会直接删行，操作前先备份** |

**冻结窗格**：选中表头的下一行 → `视图 → 冻结窗格`。滚动时表头始终可见，长表的标配。

### 2.4 文本连接：`&`

用 `&` 把多个文本拼在一起，固定文本要用英文双引号包起来。

```
=A1&"-"&B1
```

> 例：A1 是 `广东`、B1 是 `1组`，结果是 `广东-1组`。拼接单号、拼接全名都靠它。

### 2.5 练一练

在 `格式测试` 表操作（可以复制上一章的数据过去）：

1. 成交金额列设为「货币」格式，0 位小数；成交客户数列设为「数值」
2. 用条件格式：金额 ≥ 15000 标红，≤ 8000 标蓝
3. 用数据验证给"产品"列加下拉框（花呗 / 借呗）
4. 加完整边框，冻结首行
5. 筛选出「产品 = 花呗」且「成交金额 ≥ 10000」的记录
6. 按成交金额降序排列
7. 新增 G 列"业务标识"，用 `&` 把工号和产品拼成 `001-花呗` 这种形式

### ✅ 参考答案（做完再看）

- 条件格式两条规则：单元格值 `>=` 15000（红填充）、`<=` 8000（蓝填充）
- 筛选「花呗 + ≥10000」共 **4 条**（16800、18500、17500、12600）
- 降序后第一行应为 **18500**
- 业务标识公式（写在 G2，然后向下填充）：

```text
=B2&"-"&C2
```

- 下拉框来源填：`花呗,借呗`（**英文逗号**，中文逗号会被当成一个整体选项）

---
## 第 3 章 基础函数

> 🟡 **本章场景**：要回答"总共多少""平均多少""最大是多少"这类问题。
> 🟡 **学完你能交付**：一个带统计汇总区的明细表。

### 3.1 公式的三条规则

1. **必须以 `=` 开头**。直接输入 `SUM(A1:A10)` 只是文本，不会算。
2. **运算符**：加 `+`、减 `-`、乘 `*`、除 `/`、乘方 `^`；比较用 `>` `<` `>=` `<=` `<>`（不等于）。
3. **函数结构**：`=函数名(参数1, 参数2, …)`，参数之间用**英文逗号**。

> 🔴 **全角符号是新手第一大坑**
> `≥` `≤` `≠` 是中文字符，Excel 不认识，写进公式会报错或者被当成文本。必须写 `>=` `<=` `<>`。
> 同理：中文逗号 `，`、中文引号 `“”`、中文括号 `（）` 都要换成英文半角的。
> **判断方法**：看输入法的月亮/太阳标志，切到英文半角再写公式。

### 3.2 五个统计函数

| 函数 | 作用 | 注意点 |
| --- | --- | --- |
| `SUM(区域)` | 求和 | 文本、空白自动忽略 |
| `AVERAGE(区域)` | 平均 | 同上，空白不计入分母 |
| `MAX` / `MIN` | 最大 / 最小 | 文本不参与 |
| `COUNT(区域)` | 计数 | **只数数字**，文本和空白不算 |
| `COUNTA(区域)` | 计数 | 数**所有非空**，文本也算 |

> 🟡 想数"一共几条记录"，用 `COUNTA` 数一个必然有值的列（如日期列）。用 `COUNT` 数文本列会得到 0。

### 3.3 引用类型：公式能不能复制的关键

| 类型 | 写法 | 复制时会怎样 | 用在哪 |
| --- | --- | --- | --- |
| 相对引用 | `A1` | 行号、列标都跟着变 | 默认，逐行计算 |
| 绝对引用 | `$A$1` | 完全不动 | 固定的阈值、税率、汇总区 |
| 混合引用 | `$A1` / `A$1` | 只锁列 / 只锁行 | 交叉表、乘法表 |

按 **F4** 在四种状态间循环切换：`$A$1` → `A$1` → `$A1` → `A1`。

跨表引用：`=SUM(数据录入!E2:E11)`；工作表名带空格或以数字开头时要加单引号：`='1月数据'!E2:E11`。

### 3.4 填充公式

写好第一个公式后，**双击单元格右下角的填充柄**，自动向下填到底（前提是旁边列有数据）。或者选中目标区域按 `Ctrl+D` 向下填充、`Ctrl+R` 向右填充。

### 3.5 练一练

在 `数据录入` 表继续做（数据在第 2–11 行）：

1. 在 A13 起建一个汇总区，用公式算出：总成交额、平均成交额、最大单笔、最小单笔、成交笔数、客户数合计
2. 新增 H 列「客单价」，`=E2/D2`，保留 2 位小数，向下填充
3. 想一想：`AVERAGE(H2:H11)` 和 `SUM(E2:E11)/SUM(D2:D11)` 结果一样吗？为什么？

### ✅ 参考答案（做完再看）

汇总区公式（标签写在 A13:A18，结果写在 B 列）：

```text
总成交额    =SUM(E2:E11)
平均成交额  =AVERAGE(E2:E11)
最大单笔    =MAX(E2:E11)
最小单笔    =MIN(E2:E11)
成交笔数    =COUNTA(A2:A11)
客户数合计  =SUM(D2:D11)
```

| 指标 | 结果 |
| --- | --- |
| 总成交额 | **127200** |
| 平均成交额 | **12720** |
| 最大单笔 | **18500** |
| 最小单笔 | **6300** |
| 成交笔数 | **10** |
| 客户数合计 | **232** |

- 客单价（H2，向下填充）：`=E2/D2`
  10 行结果依次为 600.00、500.00、578.12、514.29、515.79、528.00、583.33、525.00、547.83、525.93
- **两个算法不一样**：
  - `AVERAGE(H2:H11)` = **541.83** —— 每笔客单价的算术平均
  - `SUM(E2:E11)/SUM(D2:D11)` = **548.28** —— 整体客单价（加权）
  - 差别来自：金额大的那几笔客户数也多，算术平均没有体现这个权重

> 🔴 **结论**：算比率类指标（客单价、转化率、毛利率），**永远用「总和 ÷ 总和」，不要用平均值**。

---

## 第 4 章 常用函数

> 🟡 **本章场景**：不只问"总共多少"，还要问"花呗总共多少""超过 15000 的有几笔""这个工号是谁"。
> 🟡 **学完你能交付**：一个带条件汇总区和查询区的明细表。

### 4.1 逻辑判断：IF / AND / OR

```text
=IF(条件, 条件成立时的值, 条件不成立时的值)
```

多档判断用嵌套：

```text
=IF(E2>=15000,"大额",IF(E2>=10000,"中额","小额"))
```

> 🟡 嵌套要**从大到小**或**从小到大**依次判断。顺序写反，后面的分支永远走不到。

多条件同时成立用 `AND`，任一成立用 `OR`：

```text
=IF(AND(C2="花呗",E2>=15000),"重点","普通")
```

### 4.2 条件统计：SUMIF / SUMIFS / COUNTIF / COUNTIFS

这是日常用得最多、也**最容易记混参数顺序**的一组。

**单条件**

```text
=SUMIF(条件区域, 条件, [求和区域])
=COUNTIF(条件区域, 条件)
```

**多条件**

```text
=SUMIFS(求和区域, 条件区域1, 条件1, 条件区域2, 条件2, ...)
=COUNTIFS(条件区域1, 条件1, 条件区域2, 条件2, ...)
```

> 🔴 **关键区别（90% 的人第一次都写错）**
> - `SUMIF`：**条件区域在前**，求和区域在最后（第 3 参数）
> - `SUMIFS`：**求和区域在前**，条件区域在后，而且可以无限加条件
> - 带 `S` 的才支持多条件，但参数顺序是反过来的

**条件的写法**

| 想表达 | 写法 | 说明 |
| --- | --- | --- |
| 等于文本 | `"花呗"` | 文本要加英文双引号 |
| 大于数字 | `">=15000"` | **比较符和数字一起放进引号** |
| 引用单元格 | `">="&H1` | 比较符在引号里，单元格用 `&` 拼在外面 |
| 不等于 | `"<>借呗"` | |

> 🟡 **为什么必须 `">="&H1`**：写成 `">=H1"` 的话，Excel 会把 `H1` 当成两个字符，而不是单元格地址。

### 4.3 查找：VLOOKUP 与 XLOOKUP

在讲查找之前先确认一件事：**查找公式几乎一定要用绝对引用锁定查找区域**。因为公式要向下复制，区域若是相对的，复制后区域跟着往下跑，结果就错了。

**VLOOKUP（老将，必须会，因为别人的表里全是它）**

```text
=VLOOKUP(查找值, 查找区域, 返回第几列, 匹配方式)
```

- 查找值必须在**查找区域的第一列**
- 匹配方式填 `0`（或 `FALSE`）是精确匹配；**填 1 或省略是模糊匹配，会给出错误结果**
- 例：`=VLOOKUP(G1,$B$2:$F$11,4,0)` —— 在 B:F 里找 G1 的工号，返回第 4 列

**VLOOKUP 的三个硬伤**

1. 只能**从左往右查**，查找列必须在最左边
2. 第 3 参数是**写死的数字**，在查找区域中间插一列就全错
3. 找不到时返回 `#N/A`，还得再套一层 `IFERROR`

**XLOOKUP（新王，能用就用它）**

```text
=XLOOKUP(查找值, 查找数组, 返回数组, [找不到时返回什么])
```

- 查找列和返回列**分开写**，可以反向查找
- 不写列序号，插入列也不怕
- 第 4 参数直接内置了容错

```text
=XLOOKUP(G1,$B$2:$B$11,$C$2:$C$11,"未找到")
```

> 🟡 XLOOKUP 只有 **Excel 2021 / 365 / 2024** 有。老版本继续用 VLOOKUP。

### 4.4 练一练

1. 用 `SUMIF` 分别算出花呗、借呗的成交总额
2. 用 `COUNTIF` 算出：成交金额 ≥ 15000 的有几笔；产品 = 花呗 的有几笔
3. 用 `SUMIF` 算出 001 号销售的成交总额
4. 用 `SUMIFS` 算出「广东-1组 + 花呗」的成交总额
5. 新增 I 列「规模标签」：金额 ≥15000 记"大额"，≥10000 记"中额"，否则"小额"
6. 建一个工号汇总区：列出 001/002/003，用 `SUMIF` + 绝对引用算出各自成交额
7. 在汇总区旁边做查询：输入工号，用 `XLOOKUP` 查出该工号成交额（第 4 参数写"未找到"）

### ✅ 参考答案（做完再看）

```text
花呗总额      =SUMIF(C2:C11,"花呗",E2:E11)
借呗总额      =SUMIF(C2:C11,"借呗",E2:E11)
大额笔数      =COUNTIF(E2:E11,">=15000")
花呗笔数      =COUNTIF(C2:C11,"花呗")
001 总额      =SUMIF(B2:B11,"001",E2:E11)
广东1组花呗   =SUMIFS(E2:E11,F2:F11,"广东-1组",C2:C11,"花呗")
```

| 题目 | 结果 |
| --- | --- |
| 花呗总额 | **75200** |
| 借呗总额 | **52000** |
| 成交金额 ≥15000 的笔数 | **3**（合计 52800） |
| 产品 = 花呗 的笔数 | **5** |
| 001 号销售总额 | **59300** |
| 广东-1组 + 花呗 | **34300** |

- 规模标签（I2，向下填充）：

```text
=IF(E2>=15000,"大额",IF(E2>=10000,"中额","小额"))
```

结果：大额 **3** 笔、中额 **4** 笔、小额 **3** 笔。

- 工号汇总区（工号写在 K2:K4，公式写在 L2 再向下填充，**区域必须绝对引用**）：

```text
=SUMIF($B$2:$B$11,K2,$E$2:$E$11)
```

| 工号 | 成交额 |
| --- | --- |
| 001 | **59300** |
| 002 | **23600** |
| 003 | **44300** |

- 查询（工号输在 N1）：

```text
=XLOOKUP(N1,K2:K4,L2:L4,"未找到")
```

> 🔴 **为什么要先汇总再查找**：直接 `=XLOOKUP(N1,B2:B11,E2:E11)` 只会返回**第一条**匹配的记录（001 → 16800，而不是 59300）。查找函数返回的是"某一条"，不是"合计数"。要合计，先用 SUMIF 汇总，再去查汇总表。

---

## 第 5 章 高级函数

> 🟡 **本章场景**：公式报错要兜底、文本要截取、要多条件但不想加辅助列、要看排名和分布。

### 5.1 IFERROR：给公式兜底

```text
=IFERROR(原公式, 出错时显示什么)
```

```text
=IFERROR(VLOOKUP(A2,$D:$E,2,0),0)
```

> 🟡 常见用法：查不到时返回 `0` 或 `""`（空文本），避免 `#N/A` 污染后面的求和运算。

### 5.2 文本处理

| 函数 | 作用 | 例子 |
| --- | --- | --- |
| `LEFT(文本,N)` | 从左边取 N 个字符 | `=LEFT(F2,2)` → `广东` |
| `RIGHT(文本,N)` | 从右边取 N 个字符 | `=RIGHT(F2,2)` → `1组` |
| `MID(文本,起点,N)` | 从中间取 | `=MID(F2,4,1)` → `1` |
| `LEN(文本)` | 字符数 | 查位数、查多余空格 |
| `TRIM(文本)` | 去掉首尾空格 | VLOOKUP 匹配不上的头号嫌疑 |
| `TEXT(数值,"格式")` | 按格式转成文本 | `=TEXT(B2,"000")` → `001` |
| `FIND(找什么,在哪找)` | 返回位置 | 配合 MID 做不规则截取 |

> 🟡 `TEXT(B2,"000")` 是保住工号前导零最稳的做法。注意结果是**文本**，不能再参与数值计算。

### 5.3 SUMPRODUCT：不加辅助列的多条件求和

```text
=SUMPRODUCT((条件区域1=条件1)*(条件区域2=条件2)*求和区域)
```

```text
=SUMPRODUCT((F2:F11="广东-1组")*E2:E11)
```

原理：`(F2:F11="广东-1组")` 得到一串 TRUE/FALSE，乘法把它们转成 1/0，不符合条件的行乘以 0 就被清掉了。

> 🟡 数据量大时 SUMPRODUCT 比 SUMIFS 慢。能用 SUMIFS 就用 SUMIFS。

### 5.4 动态数组（Excel 365）

一个公式返回一片结果，自动"溢出"到相邻单元格。

| 函数 | 作用 |
| --- | --- |
| `UNIQUE(区域)` | 去重，返回唯一值列表 |
| `SORT(区域, 第几列, -1)` | 排序，-1 为降序 |
| `FILTER(区域, 条件, [无结果时])` | 按条件筛出一整片记录 |

```text
=UNIQUE(F2:F11)
=FILTER(A2:F11,C2:C11="花呗","无")
=SORT(UNIQUE(F2:F11))
```

> 🟡 这三件套配合起来，能做出"输入条件 → 自动出结果表"的动态报表，不用每次手动筛选再复制。

### 5.5 排名与分布

| 函数 | 作用 |
| --- | --- |
| `RANK.EQ(数值, 区域, 0)` | 排名，第 3 参数 0 降序 / 1 升序 |
| `LARGE(区域,N)` / `SMALL(区域,N)` | 第 N 大 / 第 N 小 |
| `MEDIAN(区域)` | 中位数 |
| `PERCENTILE.INC(区域,0.25)` | 分位数（0.25 = Q1，0.75 = Q3） |
| `STDEV.S(区域)` | 样本标准差（看波动） |

> 🔴 **中位数 vs 平均数**：平均数会被极端值拉跑。收入、房价、客单价这类右偏数据，**看中位数更靠谱**。

### 5.6 练一练

1. 用 `LEFT` 从"业务组"里提取省份（广东 / 广西）
2. 用 `TEXT` 把工号统一成三位（001）
3. 用 `SUMPRODUCT` 算广东-1组的成交额
4. 用 `RANK.EQ` 给每笔成交金额降序排名
5. 用 `MEDIAN`、`PERCENTILE.INC`、`STDEV.S` 看成交金额的分布
6. （365）用 `UNIQUE` 列出所有业务组；用 `FILTER` 把所有花呗记录筛出来

### ✅ 参考答案（做完再看）

```text
省份        =LEFT(F2,2)
三位工号    =TEXT(B2,"000")
广东1组额   =SUMPRODUCT((F2:F11="广东-1组")*E2:E11)
排名        =RANK.EQ(E2,$E$2:$E$11,0)
```

- 省份提取结果：广东、广东、广西、广东、广东、广西、广东、广东、广西、广东
- 广东-1组成交额：**59300**
- 排名（按表格原顺序）：**3、9、1、7、8、5、2、10、6、4**
- 分布：中位数 **12900**、Q1 **10050**、Q3 **16150**、标准差 **4163.55**
- `UNIQUE(F2:F11)` 返回 3 个：广东-1组、广东-2组、广西-1组
- `FILTER(A2:F11,C2:C11="花呗")` 返回 **5** 行

---

## 第 6 章 数据透视表

> 🟡 **本章场景**：老板要"按业务组、按产品看成交额"。不用写公式，拖几下就出来。
> 🟡 **学完你能交付**：一张可交互的多维汇总表 + 切片器。

### 6.1 建一个透视表

选中数据区（或智能表内任意单元格）→ `插入 → 数据透视表` → 确定。

> 🟢 **前提**：数据源必须是第 0 章说的一维表 —— 表头唯一、无合并单元格、无空行。不满足就先回去修数据。

### 6.2 四个区域

| 区域 | 放什么 | 效果 |
| --- | --- | --- |
| 行 | 分类字段（业务组、产品） | 纵向展开 |
| 列 | 分类字段（月份） | 横向展开 |
| 值 | 要算的字段（成交金额） | 默认求和，可改计数/平均 |
| 筛选 | 全局过滤字段 | 整表生效 |

**值字段设置**：点值区域的下拉 → 值字段设置 → 可切换**求和 / 计数 / 平均值 / 最大值**，数字格式也在这里改。

> 🟡 文本字段拖进去默认是"计数"，数字字段默认是"求和"。想改就点"值字段设置"。

### 6.3 组合：把日期聚成月 / 季度

右键透视表里的日期 → **组合** → 选择「月」「季度」「年」。

> 这是透视表最实用的一招：明细是每天的，右键一下就变成按月看。

### 6.4 值显示方式：让透视表直接算占比

值字段设置 → **值显示方式**：

| 显示方式 | 含义 | 用在哪 |
| --- | --- | --- |
| 占总和的百分比 | 每行占总计的 % | 看结构占比 |
| 父行汇总的百分比 | 在上一级分类里的 % | 分层看内部构成 |
| 按某一字段汇总 | 逐行累加 | 画帕累托图 |
| 差异 / 差异百分比 | 与指定项比较 | 对比基准 |

### 6.5 切片器与日程表

`透视表分析 → 插入切片器`（业务组、产品）、`插入日程表`（日期）。

> 切片器就是把筛选做成按钮，点一下换一版数据。做汇报时比下拉筛选好用得多。多个透视表可以共用一个切片器（右键切片器 → 报表连接）。

### 6.6 筛选之后求和：SUBTOTAL 而不是 SUM

在**明细表**上筛选后，`SUM` 会把被隐藏的行也算进去。要只算看得见的行：

```text
=SUBTOTAL(109, E2:E11)
```

`109` = 求和且忽略隐藏行；`9` = 求和但包含隐藏行。

> 🔴 这是"筛选后合计对不上"这个经典问题的唯一正解。第一个参数换成 `101` 是平均值、`104` 是最大值。

### 6.7 计算字段 vs 计算项

- **计算字段**：在透视表里新增一个由现有字段算出来的字段（如 `=成交金额/成交客户数`）。入口：`透视表分析 → 字段、项目和集 → 计算字段`。
- **计算项**：在**某个字段内部**新增一项。

> 🔴 **能不算就不算**：计算字段是逐行算完再聚合，结果经常和"先求和再相除"不一样（又一个加权 vs 算术平均的坑）。**比率类指标优先在源数据里做成一列，或者放到 Power Pivot 用度量值。**

### 6.8 练一练

1. 建透视表：行 = 业务组，值 = 成交金额（求和）
2. 再加一个「占总和的百分比」的值字段
3. 改成：行 = 业务组，列 = 产品，值 = 成交金额
4. 另建一个：行 = 产品，值 = 成交金额 + 成交客户数
5. 插入切片器（业务组），点一下看联动
6. 回到明细表，筛选出"花呗"，用 `SUBTOTAL(109,...)` 求和，再和 `SUM` 对比

### ✅ 参考答案（做完再看）

**按业务组**

| 业务组 | 成交额 | 占比 |
| --- | --- | --- |
| 广东-1组 | **59300** | 46.6% |
| 广西-1组 | **44300** | 34.8% |
| 广东-2组 | **23600** | 18.6% |
| 总计 | **127200** | 100% |

**业务组 × 产品**

| 业务组 | 花呗 | 借呗 | 合计 |
| --- | --- | --- | --- |
| 广东-1组 | 34300 | 25000 | 59300 |
| 广东-2组 | 9800 | 13800 | 23600 |
| 广西-1组 | 31100 | 13200 | 44300 |
| 合计 | 75200 | 52000 | 127200 |

**按产品**

| 产品 | 成交额 | 占比 | 客户数 |
| --- | --- | --- | --- |
| 花呗 | 75200 | 59.1% | 132 |
| 借呗 | 52000 | 40.9% | 100 |

- 筛选"花呗"后：`SUBTOTAL(109,E2:E11)` = **75200**；`SUM(E2:E11)` 仍然 = **127200**

---

## 第 7 章 Power Query：把重复劳动一次性做掉

> 🟡 **本章场景**：每个月都要把新数据复制进来、改格式、删列、合并，做一遍半小时。
> 🟡 **学完你能交付**：一个"下个月只要点刷新"的自动化数据管道。
> 🔴 **WPS 用户**：本章功能在 WPS 中不支持或支持有限，请用 Excel 桌面版学习。

### 7.1 它解决什么问题

Power Query（PQ）记录的是**一组操作步骤**，而不是结果。你在界面上点一次"拆分列"，它就记一步；下个月数据换了，点**刷新**，所有步骤自动重跑。

> 🔴 **最关键的一句话**：PQ 的所有操作**不修改原始数据**，原始数据永远安全。

### 7.2 导入数据

`数据 → 获取数据 → 从文件 → 从工作簿`（或从表格/区域）。选中表 → 点「转换数据」进入 PQ 编辑器。

### 7.3 常用转换

| 操作 | 入口 | 说明 |
| --- | --- | --- |
| 更改类型 | 点列标左边的图标 | **第一步就该做**：日期设为"日期"、金额设为"整数" |
| 删除列 | 右键列 → 删除 | |
| 筛选 | 列标下拉 | 和 Excel 里一样 |
| 拆分列 | 转换 → 拆分列 | 按分隔符（如 `-`）或按字符数 |
| 替换值 | 右键列 → 替换值 | 批量修正错别字 |
| 添加自定义列 | 添加列 → 自定义列 | 写公式生成新列 |

> 🟡 右侧的「应用的步骤」是 PQ 的灵魂：点任一步可回看，点某一步前的叉可撤销，还能拖拽调整顺序。

### 7.4 逆透视：把二维交叉表转回一维表

这是 PQ 最"救命"的功能。老板给的报表往往长这样：

| 销售工号 | 花呗 | 借呗 |
| --- | --- | --- |
| 001 | 34300 | 25000 |
| 002 | 9800 | 13800 |
| 003 | 31100 | 13200 |

这种表 Excel 分析不了。用逆透视：

1. 选中 `销售工号` 列 → 右键 → **逆透视其他列**
2. 瞬间变成：

| 销售工号 | 属性 | 值 |
| --- | --- | --- |
| 001 | 花呗 | 34300 |
| 001 | 借呗 | 25000 |
| 002 | 花呗 | 9800 |
| 002 | 借呗 | 13800 |
| 003 | 花呗 | 31100 |
| 003 | 借呗 | 13200 |

3. 把「属性」改名为「产品」，「值」改名为「成交金额」，搞定。

> 🟢 从"人看着舒服的报表"变回"Excel 能算的数据"，就靠这一步。

### 7.5 合并查询：VLOOKUP 的工业版

场景：明细表只有工号，想带上姓名。

1. 建一张维度表（工号 → 姓名、业务组），也导入 PQ
2. 在明细表查询里：`开始 → 合并查询`
3. 选匹配的列（两边都点「销售工号」），联接种类选「左外部」
4. 展开新列，勾选「姓名」

**比 VLOOKUP 强在哪**：不用写公式、不怕插入列、不会因为格式不一致匹配失败、几十万行也不卡。

维度表（可直接复制练习）：

| 销售工号 | 姓名 | 业务组 |
| --- | --- | --- |
| 001 | 张伟 | 广东-1组 |
| 002 | 李娜 | 广东-2组 |
| 003 | 王强 | 广西-1组 |

### 7.6 追加查询：把多张表叠起来

`开始 → 追加查询 → 将查询追加为新查询`，把 1月、2月、3月三个结构相同的表叠成一张。

> 🟡 前提是**列名和顺序一致**。不一致的话 PQ 会生成 null，先把列名统一。

### 7.7 关闭并上载

`开始 → 关闭并上载`，数据回到 Excel。以后源数据更新了 → `数据 → 全部刷新`。

> 🟢 数据量大时选「关闭并上载至 → 仅创建连接 / 加载到数据模型」，别把几十万行明细全铺到工作表里。

---

## 第 8 章 Power Pivot：Excel 里的"小型数据库"

> 🟡 **本章场景**：数据有几十万行，或者要跨多张表算指标。
> 🟡 **学完你能交付**：一个带关系的迷你数据模型 + 一组可复用的度量值。
> 🔴 **WPS 用户**：本章功能在 WPS 中不支持，请用 Excel 桌面版（Professional Plus / 365）。

### 8.1 先建"数据模型"

Power Pivot 的核心思想：**不要把所有东西塞进一张大表**。

- **事实表**：记录"发生了什么"（每笔成交），行数多
- **维度表**：描述"谁 / 什么 / 哪里"（销售、产品、业务组），行数少

两者用关键字段（工号、产品名）关联，形成**星型模型**。

> 🟡 好处：改一次维度表（比如业务组改名），所有报表自动跟着变；事实表也不会被重复的描述信息撑爆。

### 8.2 启用与加载

`Power Pivot → 管理`（若功能区没有：`文件 → 选项 → 加载项 → COM 加载项 → 勾选 Microsoft Power Pivot`）。

加载数据：`Power Pivot → 添加到数据模型`，或在 PQ 里选「加载到数据模型」。

### 8.3 建立关系

在 Power Pivot 窗口切到**关系图视图**，把维度表的「销售工号」拖到事实表的「销售工号」上。

> 🔴 两表的关键字段**数据类型必须一致**（都是文本或都是数字），否则关系建不上。

### 8.4 计算列 vs 度量值

| | 计算列 | 度量值 |
| --- | --- | --- |
| 算在哪 | 每一行 | 整个筛选上下文 |
| 何时算 | 加载时，占内存 | 用到时才算 |
| 适合 | 行级属性（如本行客单价） | 汇总指标（总额、占比、同比） |

> 🔴 **判断标准**：这个东西要不要**在透视表里按维度拆开来看**？要 → 度量值；不要 → 计算列。比率类指标几乎都是度量值。

### 8.5 四个够用的 DAX 函数

```text
总成交额 := SUM('明细'[成交金额])
笔均金额 := DIVIDE([总成交额], COUNTROWS('明细'))
客单价   := DIVIDE([总成交额], SUM('明细'[成交客户数]))
花呗额   := CALCULATE([总成交额], '明细'[产品]="花呗")
```

- `SUM` 和 Excel 里一样，但作用在**整列**
- `DIVIDE(分子, 分母, [除不尽时返回])` —— **自带除零保护**，比直接写 `/` 稳
- `CALCULATE(度量值, 筛选条件)` —— DAX 里最重要的函数，作用是"在指定条件下重新计算这个度量值"
- 度量值用 `[方括号]` 引用，列用 `'表名'[列名]`

> 🟡 度量值放进透视表的值区域，会**跟着行 / 列 / 切片器自动重算**。这就是它比 Excel 公式强的地方：写一个，处处能用。

### 8.6 练一练

1. 把明细表加载到数据模型
2. 建 4 个度量值：总成交额、成交笔数、客单价（总额 ÷ 总客户数）、花呗占比
3. 建透视表：行 = 业务组，值 = 总成交额 + 客单价
4. 加切片器（产品），看看客单价怎么变

### ✅ 参考答案（做完再看）

```text
总成交额   := SUM('明细'[成交金额])
成交笔数   := COUNTROWS('明细')
客单价     := DIVIDE([总成交额], SUM('明细'[成交客户数]))
花呗占比   := DIVIDE(CALCULATE([总成交额],'明细'[产品]="花呗"), [总成交额])
```

| 度量值 | 结果 |
| --- | --- |
| 总成交额 | **127200** |
| 成交笔数 | **10** |
| 客单价 | **548.28** |
| 花呗占比 | **59.1%** |

按业务组拆开后，客单价：广东-1组 **559.43**、广西-1组 **553.75**、广东-2组 **513.04**。

> 注意：这里客单价是"该组总额 ÷ 该组总客户数"，不是各行客单价的平均 —— 这正是度量值自动帮你做对的事。

---

## 第 9 章 综合实战：从一张原始表到一句结论

> 🟡 **本章场景**：老板丢来一句话 —— "上个月做得怎么样？哪个组最好？花呗和借呗哪个更值得投入？"

### 9.1 完整流程

| 步骤 | 做什么 | 用第几章 |
| --- | --- | --- |
| 1 | 检查数据规范性 | 第 0 章：一维表、Ctrl+T、去脏数据 |
| 2 | 建辅助列（客单价、规模标签、省份） | 第 3–5 章 |
| 3 | 出总量和结构 | SUM / SUMIF / COUNTIF |
| 4 | 多维交叉 | 第 6 章 透视表 |
| 5 | 加占比、排名 | 值显示方式 / RANK |
| 6 | 做成可刷新 | 第 7、8 章 |
| 7 | 说人话，给结论 | —— |

### 9.2 动手

用第 1 章那份 10 行数据（真实场景数据量更大，但步骤完全一样）。

**Step 1 总量**

总成交额 **127200**，10 笔，平均单笔 **12720**，整体客单价 **548.28**。

**Step 2 按业务组拆**

| 业务组 | 成交额 | 占比 | 客户数 | 客单价 |
| --- | --- | --- | --- | --- |
| 广东-1组 | 59300 | 46.6% | 106 | 559.43 |
| 广西-1组 | 44300 | 34.8% | 80 | 553.75 |
| 广东-2组 | 23600 | 18.6% | 46 | 513.04 |

**Step 3 按产品拆**

| 产品 | 成交额 | 占比 | 客户数 | 客单价 |
| --- | --- | --- | --- | --- |
| 花呗 | 75200 | 59.1% | 132 | 569.70 |
| 借呗 | 52000 | 40.9% | 100 | 520.00 |

**Step 4 交叉下钻**

| 业务组 | 花呗 | 借呗 | 合计 |
| --- | --- | --- | --- |
| 广东-1组 | 34300 | 25000 | 59300 |
| 广西-1组 | 31100 | 13200 | 44300 |
| 广东-2组 | 9800 | 13800 | 23600 |
| 合计 | 75200 | 52000 | 127200 |

**Step 5 分布与风险**

成交金额中位数 **12900**、Q1 **10050**、Q3 **16150**、标准差 **4163.55**；单笔最大 **18500**、最小 **6300**，跨度接近 3 倍。

### 9.3 写成结论

> 🔵 **结论（照着这个结构写）**
> 1. **总量**：10 笔成交，总额 127200，客单价 548.28。
> 2. **集中度高**：广东-1组 + 广西-1组 合计占 **81.4%**，前两组决定大盘。
> 3. **产品倾斜**：花呗 75200，占 **59.1%**，客单价 569.70 也高于借呗的 520.00 —— **花呗更值得投入**。
> 4. **最大单点**：广东-1组的花呗 34300，占全盘 **27.0%**，是必须守住的基本盘。
> 5. **短板**：广东-2组只占 18.6%，客单价 513.04 也是三个组里最低 —— 属于**量价双低**，需要单独排查（是新组、还是区域问题）。
> 6. **风险提示**：样本只有 10 笔，金额跨度近 3 倍，波动不小；建议在下月数据到齐后复核。

> 🟢 **汇报的铁律：结论先行**。先说答案，再说依据。老板问"怎么样"，第一句就该是"花呗更值得投入，占 59.1% 且客单价更高"，而不是"我先做了个透视表……"。

---

## 附录 A 报错排错手册

| 报错 | 含义 | 常见原因 | 怎么修 |
| --- | --- | --- | --- |
| `#N/A` | 找不到 | VLOOKUP / XLOOKUP 没匹配上 | 查**空格**（用 TRIM）、文本型数字、查找值是否在第一列 |
| `#VALUE!` | 类型不对 | 把文本当数字算 | 分列转成数字；检查是否混入全角符号 |
| `#REF!` | 引用失效 | 删了被引用的行 / 列 / 表 | `Ctrl+Z` 撤销，或重设公式区域 |
| `#DIV/0!` | 除以 0 | 分母为空或为 0 | 用 `IFERROR` 或 `DIVIDE` 兜底 |
| `#NAME?` | 函数名不认识 | 拼错函数名、用了中文引号 | 检查拼写和符号全半角 |
| `#####` | 不是错误 | 列宽不够 | 拉宽列；日期算成负数也会显示这个 |
| 循环引用 | 公式引用了自己 | 在 A1 写 `=A1+1` | `公式 → 错误检查 → 循环引用` 定位 |

> 🟡 **万能排查三步**
> 1. 选中出错单元格 → `公式 → 公式求值`，一步步看中间结果
> 2. `公式 → 显示公式`（`Ctrl+~`），一眼看出哪几行的区域跑偏了
> 3. 检查**全半角**：把公式里的符号全部换成英文的，一半的错就没了

## 附录 B 函数速查

| 分类 | 函数 | 一句话 |
| --- | --- | --- |
| 统计 | `SUM` `AVERAGE` `MAX` `MIN` | 求和 / 平均 / 最大 / 最小 |
| 计数 | `COUNT` `COUNTA` `COUNTIF` `COUNTIFS` | 数数字 / 数非空 / 单条件 / 多条件 |
| 条件求和 | `SUMIF` `SUMIFS` `SUMPRODUCT` | 单条件 / 多条件 / 数组式多条件 |
| 逻辑 | `IF` `AND` `OR` `IFERROR` | 判断 / 且 / 或 / 容错 |
| 查找 | `VLOOKUP` `XLOOKUP` | 老版 / 新版（推荐） |
| 文本 | `LEFT` `RIGHT` `MID` `LEN` `TRIM` `TEXT` `FIND` `&` | 截取、清洗、格式化、拼接 |
| 排名分布 | `RANK.EQ` `LARGE` `SMALL` `MEDIAN` `PERCENTILE.INC` `STDEV.S` | 名次、分位、波动 |
| 可见行求和 | `SUBTOTAL(109,…)` | **只算看得见的行** |
| 动态数组 | `UNIQUE` `SORT` `FILTER` | 365 专属，一个公式出一片 |
| 日期 | `TODAY` `DATEDIF` `EDATE` `EOMONTH` `MONTH` | 今天 / 间隔 / 加减月 / 月末 / 取月份 |
| DAX | `SUM` `DIVIDE` `CALCULATE` `COUNTROWS` | Power Pivot 四件套 |

## 附录 C 快捷键

| 分类 | 快捷键 | 作用 |
| --- | --- | --- |
| 文件 | `Ctrl+N` / `Ctrl+S` / `F12` | 新建 / 保存 / 另存为 |
| 编辑 | `Ctrl+C` `Ctrl+V` `Ctrl+X` `Ctrl+Z` `Ctrl+Y` | 复制 / 粘贴 / 剪切 / 撤销 / 重做 |
| 填充 | `Ctrl+D` / `Ctrl+R` | 向下填充 / 向右填充 |
| 选中 | `Ctrl+Shift+↓` / `Ctrl+A` | 选到列底 / 全选当前区域 |
| 表格 | `Ctrl+T` | 建智能表（**最重要**） |
| 筛选 | `Ctrl+Shift+L` | 加 / 去筛选 |
| 查找 | `Ctrl+F` / `Ctrl+H` | 查找 / 替换 |
| 公式 | `F4` | 切换绝对 / 相对引用 |
| 公式 | `Ctrl+~` | 显示所有公式 |
| 日期 | `Ctrl+;` | 输入当天日期 |
| 快速填充 | `Ctrl+E` | 闪电填充：给一个样例，自动识别规律填满整列（拆分、合并、格式转换的神器） |
| 定位 | `Ctrl+G` → 定位条件 | 批量选中空值、公式、可见单元格 |
| 粘贴 | `Ctrl+Alt+V` | 选择性粘贴（转数值、转置、运算） |

> 🟢 **最值得先记住的 5 个**：`Ctrl+T`（智能表）、`Ctrl+Shift+L`（筛选）、`F4`（绝对引用）、`Ctrl+E`（闪电填充）、`Ctrl+Alt+V`（选择性粘贴）。这五个能省掉你一半的操作时间。

---

> 🟡 **到这里**：这份文档覆盖了从"建一张规范的表"到"做出可刷新的数据模型"的完整路径。
> 如果某处算不出来，先回到第 0 章检查数据规范性 —— 九成的"高级功能用不了"，根源都在数据不规范。
