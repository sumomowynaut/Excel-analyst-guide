# 更新日志

记录仓库的**结构性变更**：文件增删、答案修正、许可与工程配置。

教程内容本身的版本递进关系见 [版本演进.md](%E7%89%88%E6%9C%AC%E6%BC%94%E8%BF%9B.md)。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)。

## [3.0.2] — 2026-09-19

### Fixed

- **`practice-data.xlsx` 全部单元格被写成文本**：日期列改为真日期（格式 `yyyy/m/d`）、数量与金额列改为数值，
  销售工号保留文本以免丢失前导零。修复后 `SUM`、日期计算、透视表分组可直接使用。
- **`交叉表练习` 与 v3.0 第 3 章正文不一致**：原表按销售工号 × 月份（合计 113700，逆透视 9 行），
  已改为按业务组 × 月份（合计 **287100**，逆透视 **12 行**），与正文第 352–375 行完全一致。
- **`说明` / `维度表` / `交叉表练习` 的章节号仍是 v2.0 口径**：`第 7 章` 已改为 `第 3 章`。
- **`case-data.xlsx` 的 `2-用户RFM` 多出 `分析基准日` 列**：已删除 F 列，与正文 5 列表结构一致，
  保留"学员自己在 H1 输入基准日"的教学设计；`最近下单日期` 改为真日期。
- **`case-data.xlsx` 的 `4-销售提成` 阶梯表不可用**：原 A15:C18 为纯文本且与正文公式引用的 `$P$2:$Q$5` 不符。
  已改为 **P1:Q5 数值阶梯表**（达成率下界 0 / 0.8 / 1 / 1.2，提成比例 0 / 1% / 1.5% / 2%），
  与 `=XLOOKUP(F2,$P$2:$P$5,$Q$2:$Q$5,,-1)` 匹配。
- **实战案例 4 提成阶梯边界重叠**：原 `80%~100%`、`100%~120%` 含端点不清，统一为 `x<80% → 0`、`80%≤x<100% → 1%`、`100%≤x<120% → 1.5%`、`x≥120% → 2%`；同步修正嵌套 IF 公式与 `XLOOKUP(...,-1)` 查表口径——原写法在达成率**恰好 100%** 时两者结果不一致（嵌套 IF 给 1%、查表给 1.5%），现已统一为 1.5%。
- **v3.0 第 12 章原始数据表有一行只有 3 列**：已补齐为 6 列。
- **v3.0 第 9 章补充"区域"口径说明**：按工号映射的区域（广东 149200）与第 3 章按业务组前缀（广东 140600）口径不同，
  两种都正确，避免读者对不上数。

### Changed

- README 与版本演进中的"字数"改为"字符数（含 Markdown 标记）"，并注明统计口径。
- 版本演进中关于全角比较符号的表述改为"公式中已改为半角，说明文字中的 ≥ / ≤ / ≠ 保留"。

## [3.0.1] — 2026-09-18

### Fixed

- **修正 v3.0 的分位数答案**。`QUARTILE()` / `PERCENTILE()` 默认按 INC（含端点）计算，本数据集 n=24 时
  Q1 应为 **9500**、Q3 应为 **13900**；原稿误用了 EXC 口径的 9300 / 14500。影响第 6 章参考答案表与第 8.6 节分布表。
- **补全 v3.0 第 6 章缺失的 P90 参考答案**（16710）。原「练一练」要求算 P90，答案表未给出。
- **删除 `LICENSE.md` 中与仓库不符的描述**。原文件声明 `mkdocs.yml`、GitHub Actions 工作流采用 MIT，但仓库中并无这些文件。

### Added

- v3.0 第 6 章补充 `QUARTILE` / `PERCENTILE` 的 **INC 与 EXC 口径说明**——两组函数算法不同，这是对数时对不上的常见原因。
- `LICENSE.md` 增加 `SPDX-License-Identifier: CC-BY-NC-SA-4.0`，并说明 GitHub 侧栏显示 `Other` 的原因
  （GitHub 许可识别库不含 CC 系列协议，非填写遗漏）。
- 新增 `.gitignore`、`CONTRIBUTING.md` 与本文件。

### Verified

- 复算 v3.0 24 行主数据的全部参考答案：总额 287100、月度 92100 / 104100 / 90900、
  3 月环比 −12.68%、中位数 11350、均值 11962.5、第 8.4 节归因表、第 8.5 节帕累托 8 项累计占比 —— 均与原文一致。
- 复算 v2.0 10 行数据的分布指标：中位数 12900、Q1 10050、Q3 16150、标准差 4163.55 —— 与原文一致，无需修改。
- 初步核对 `practice-data.xlsx`、`case-data.xlsx` 的结构与正文数据表；后续发现类型与口径问题，已在 3.0.2 修复。

## [3.0.0] — 2026-09-18

### Added

- **v3.0 教程**：13 章 + 3 附录，3.3 万字，按分析师七步工作流组织
- 24 行跨 1–3 月主数据集，内置真实归因剧情（3 月环比 −12.7%）
- **实战案例包**：6 个业务场景（库存 / 用户 RFM / 预算 / 销售提成 / 渠道投放 / 客服工单）
- 配套练习数据 `practice-data.xlsx`（4 张表）、`case-data.xlsx`（6 张表）
- **速查卡**：函数 / 快捷键 / 报错 / 数据规范检查清单一页纸
- 沿用并保留 v2.0、v1.0
- 采用 CC BY-NC-SA 4.0 许可
