# Excel 从入门到精通（v3.0）：从会做表到会分析

> 🟡 **这份文档和别的 Excel 教程有什么不同**
> 大多数教程按 Excel 的**功能**讲：界面、函数、透视表、Power Query……学完你知道每个按钮在哪。
> 这份按**分析师的工作流**讲：接需求 → 取数 → 清洗 → 建指标 → **分析** → **可视化** → **汇报**。
> 学完你不只会做表，还知道拿到一个业务问题时从哪下手。

> 🟡 **怎么用**
> 1. 练习数据都在**表格**里：全选表格 → 复制 → 粘到 Excel 的 **A1** → 自动分列。也可以直接下载配套的 `practice-data.xlsx`
> 2. 答案都在 `✅ 参考答案` 标题下面，**先自己做完，再点开核对**
> 3. 每章末尾有「🤖 问 AI」，卡住了直接拿去问
>
> 🔴 **最重要的一条**：**练习请用 Excel 桌面版（2021 / 365 / 2024）**。Power Query、Power Pivot、动态数组这些在飞书表格和 WPS 里不支持，在那儿练会以为是自己学错了。

> 🟡 **版本说明**
> 基于 **Excel 2021 / 365 / 2024**。WPS 用户注意：**第 3、9 章（Power Query / Power Pivot）不支持或支持有限**，请用 Excel 桌面版学习。Mac 用户把 `Ctrl` 换成 `Cmd`。

## 分析师的七步工作流

整个教程围绕这条主线展开，你现在在哪一步、下一步是什么，随时可以对照：

| 步骤 | 要回答的问题 | 对应章节 |
| --- | --- | --- |
| 1 接需求 | 业务到底想问什么？指标怎么定义？ | 第 8、10、12 章 |
| 2 取数 | 数据从哪来、口径是什么？ | 第 0、3 章 |
| 3 清洗 | 数据脏在哪、怎么弄干净？ | 第 0、2、3 章 |
| 4 建指标 | 客单价、完成率这些怎么算？ | 第 4、5、6 章 |
| 5 分析 | 为什么涨、为什么跌、谁拖的？ | **第 7、8 章** |
| 6 可视化 | 用什么图、怎么让人一眼看懂？ | 第 10 章 |
| 7 汇报 | 所以呢？该做什么？ | 第 10、12 章 |

> 🔴 **先说清楚一件事**：大多数 Excel 教程只覆盖第 3、4 步。你会做表，但领导问"上月业绩为什么掉了 15%"，你不知道从哪下手——因为你没学过**拆解**这个动作。这份教程把重心放在第 5、6、7 步。

---

## 第 0 章 地基：先把数据做成能用的样子

> 🔵 **为什么这章最重要**：透视表、Power Query、Power Pivot 有一个共同前提——**数据源必须规范**。数据不规范，它们要么报错，要么算出莫名其妙的结果。

### 0.1 一维表：唯一正确的数据形态

**一行一条记录，一列一个字段，表头只有一行。**

**✅ 正确（一维表）**

| 成交日期 | 销售工号 | 产品 | 成交客户数 | 成交金额 | 业务组 |
| --- | --- | --- | --- | --- | --- |
| 2026/1/5 | 001 | 花呗 | 25 | 12500 | 广东-1组 |
| 2026/1/8 | 002 | 借呗 | 15 | 8900 | 广东-2组 |
| 2026/1/12 | 001 | 花呗 | 30 | 15600 | 广东-1组 |

**❌ 错误（二维交叉表，最常见的毛病）**

| 业务组 | 1月 | 2月 | 3月 |
| --- | --- | --- | --- |
| 广东-1组 | 23300 | 24000 | 10200 |
| 广东-2组 | 20700 | 16800 | 20400 |

第二种"更适合人看"，但 Excel 没法分析它：想加一列"产品"没地方加，透视表也出不来。

> 🔴 **四条铁律**
> 1. 表头只有一行，且不能重名
> 2. **不要合并单元格**（标题要合并，就在数据表外面做）
> 3. 不要有空行、空列切断数据区
> 4. 一列只放一种数据（别在金额列写"约1万"）

### 0.2 拿到二维表怎么办：逆透视

现实里你拿到的往往是上面那种"错误"表。别手工搬，**用 Power Query 的「逆透视」一键转**（详见第 3 章）。

操作一句话：选中那几个月份列 → `转换 → 逆透视列` → 二维表立刻变一维表。

> 🟢 原则（第 0 章）和工具（第 3 章）必须配对。很多教程只讲"应该是一维表"，却不教怎么转，学员拿到真实的二维表照样束手无策。

### 0.3 Ctrl+T：拿到数据第一件事

选中数据区（含表头）→ **Ctrl+T** → 确认"包含表头" → 确定。

好处：数据区自动扩展、自动隔行配色、自带筛选；更重要的是它是透视表和 Power Query 的**正当数据源**，普通区域容易错乱。

**还有一招：公式可以用列名写**。转表并给它起个名字（「表设计」→「表名称」）之后，`=SUM(E2:E11)` 可以写成 `=SUM(销售明细[成交金额])` —— 一眼看出在算什么；而且往表里追加新行时范围自动跟上，**公式不用改**（`E2:E11` 不会，得手动改成 `E2:E12`）。表内新增列写 `=[@成交金额]/[@成交客户数]`，回车自动填满整列。注意 `[@列名]` 只能在表内用。

### 0.4 三种典型脏数据的处理

| 症状 | 表现 | 怎么修 |
| --- | --- | --- |
| 文本型数字 | 左上角绿色三角，**SUM 算不进去** | 选中列 → `数据 → 分列` → 完成 |
| 多余空格 | 看着一样但 VLOOKUP 匹配不上 | `=TRIM()`；不可见字符用 `=CLEAN()` |
| 日期是文本 | 无法按月分组、排序错乱 | `=DATEVALUE()`，或分列时选"日期" |

### 0.5 练一练

判断下面三种情况分别违反了哪条铁律：① 表头用了两行；② 金额列里混了"待定"两个字；③ 中间隔了一行写"以下为二月数据"。

### ✅ 参考答案（做完再看）

- ① 违反"表头只有一行"——透视表会把第二行当成数据
- ② 违反"一列一种数据"——该列会被整体当成文本，`SUM` 直接忽略，结果偏小且不报错
- ③ 违反"无空行"——透视表只认空行以上的部分，后面的数据被静默丢弃

> 🔴 这三种都不会报错，只会**静悄悄地算错**。这是数据工作里最危险的一类问题。

### 🤖 问 AI

> "我有一张 Excel 表，想做数据透视表但结果不对。帮我列出检查清单，判断数据源是否规范。"

---

## 第 1 章 基础认知（够用即可）

> 这一章只讲**后面会用到的**操作，不追求面面俱到。界面细节遇到再查，别在这上面花时间。

### 1.1 Excel 的本质

一张由单元格组成的二维表，用"列标 + 行号"定位（`A1`、`C2`）。

它和"在 Word 里画表格"的根本区别是：**数据之间可以关联计算**——改一个数字，所有引用它的公式自动更新。

### 1.2 四个必记快捷键

| 操作 | 快捷键 |
| --- | --- |
| 保存 / 另存为 | `Ctrl+S` / `F12` |
| 转成智能表 | `Ctrl+T` |
| 自动求和 | `Alt+=` |
| 加筛选 | `Ctrl+Shift+L` |

### 1.3 状态栏：不用写公式的快速统计

选中一片数字，看窗口**右下角状态栏**——直接显示计数、求和。右键状态栏可勾选平均值、最大值、最小值。

看一眼就够的场景，别写公式。

### 1.4 两个容易后悔的操作

- **合并单元格**：能做漂亮标题，但**绝对不要用在数据区**——合并过的表无法排序、筛选，透视表直接失败
- **删除重复值**：会**真的删行**，操作前先备份

### 1.5 日期的本质是数字

数字 `1` = 1900年1月1日，`2` = 1月2日，依此类推。

理解这点的好处：日期可以做加减（算天数差）、可以按月分组；如果某天它突然显示成 `46000` 这种数字，只是单元格被设成了"常规"格式，改回"日期"即可。

### 1.6 练一练：录入本篇的主数据

新建工作簿 → 在 `数据录入` 表录入下面这份数据 → `Ctrl+T` 转智能表。

**这份 24 行数据是全篇的主数据集**，后面所有章节都基于它，位置统一：**表头第 1 行，数据第 2–25 行**。

| 成交日期 | 销售工号 | 产品 | 成交客户数 | 成交金额 | 业务组 |
| --- | --- | --- | --- | --- | --- |
| 2026/1/5 | 001 | 花呗 | 25 | 12500 | 广东-1组 |
| 2026/1/8 | 002 | 借呗 | 15 | 8900 | 广东-2组 |
| 2026/1/12 | 001 | 花呗 | 30 | 15600 | 广东-1组 |
| 2026/1/15 | 003 | 借呗 | 18 | 9200 | 广西-1组 |
| 2026/1/20 | 002 | 花呗 | 22 | 11800 | 广东-2组 |
| 2026/1/25 | 003 | 借呗 | 26 | 13500 | 广西-1组 |
| 2026/1/28 | 004 | 花呗 | 19 | 9800 | 海南-1组 |
| 2026/1/31 | 001 | 借呗 | 21 | 10800 | 广东-2组 |
| 2026/2/3 | 001 | 借呗 | 21 | 10800 | 广东-1组 |
| 2026/2/7 | 002 | 花呗 | 28 | 16800 | 广东-2组 |
| 2026/2/10 | 003 | 花呗 | 32 | 18500 | 广西-1组 |
| 2026/2/15 | 004 | 借呗 | 17 | 7500 | 海南-1组 |
| 2026/2/20 | 001 | 花呗 | 24 | 13200 | 广东-1组 |
| 2026/2/22 | 002 | 借呗 | 17 | 8600 | 广西-1组 |
| 2026/2/25 | 003 | 花呗 | 29 | 17200 | 广西-1组 |
| 2026/2/28 | 004 | 借呗 | 23 | 11500 | 海南-1组 |
| 2026/3/2 | 001 | 花呗 | 20 | 10200 | 广东-1组 |
| 2026/3/6 | 002 | 借呗 | 14 | 6800 | 广东-2组 |
| 2026/3/11 | 003 | 花呗 | 27 | 14800 | 广西-1组 |
| 2026/3/16 | 001 | 借呗 | 19 | 9600 | 广东-2组 |
| 2026/3/21 | 002 | 花呗 | 25 | 13600 | 广东-2组 |
| 2026/3/26 | 003 | 借呗 | 22 | 11200 | 广西-1组 |
| 2026/3/29 | 004 | 花呗 | 30 | 16500 | 海南-1组 |
| 2026/3/31 | 004 | 借呗 | 16 | 8200 | 海南-1组 |

> 🟡 **关于工号 `001`**：粘贴后可能显示成 `1`，这不是错了，只是被当成了数字。想保持三位显示：选中该列 → 设置单元格格式 → 自定义 → 输入 `000`。

### ✅ 参考答案（做完再看）

- 表头第 1 行，数据第 **2–25** 行，共 **24 条 × 6 列**
- 状态栏看到的成交金额：平均 **11962**、最大 **18500**、计数 **24**
- 留意一下：**工号和业务组不是一一对应的**（001 既在广东-1组也在广东-2组）。这是故意设计的，否则后面做交叉分析就没有意义了

### 🤖 问 AI

> "Excel 里粘贴 001 变成 1 了，怎么保持前面的 0？有哪几种方法，各有什么副作用？"

---

## 第 2 章 清理与整理：把数据弄干净

> 🟡 **本章场景**：数据录进来了，但格式乱、有重复、要按条件挑着看。
> 🟡 **学完你能交付**：一张格式规范、可筛选、可排序的干净明细表。

### 2.1 格式设置

- **数字格式**：金额用「货币」、增长率用「百分比」、数量用「数值」并定小数位。**这一步决定报表看起来专不专业**。
- **边框 / 填充**：数据区加边框（否则打印没线），标题行加填充色。
- **冻结窗格**：选中表头下一行 → `视图 → 冻结窗格`。长表滚屏时表头不掉，是标配。

### 2.2 条件格式进阶：最廉价的可视化

条件格式不只是"标红"。三类高级玩法：

| 类型 | 效果 | 适合场景 |
| --- | --- | --- |
| **数据条** | 单元格里画横向条形 | 一眼看出谁高谁低，替代柱状图 |
| **色阶** | 用颜色深浅表示大小 | 看矩阵分布、找热点 |
| **图标集** | 打勾/感叹/叉 | 状态标记（达标/预警/未达标） |

> 🟢 **数据条是性价比最高的可视化**：不用做图，数字旁边直接有长短条，做业绩对比时比看数字快得多。

**实操**：选中成交金额列 → `开始 → 条件格式 → 数据条` → 选一种样式。

### 2.3 Flash Fill（Ctrl+E）：AI 时代的提取神器

**场景**：从"广东-1组"里提取省份。传统做法是写 `=LEFT(F2,2)`。但有更简单的方式——

1. 在旁边空列的第一行，**手动输入**期望结果（如 `广东`）
2. 第二行再手动输入一个（`广东`）
3. 按 **`Ctrl+E`**

Excel 会识别你的意图，**自动填完剩下所有行**。

它同样能处理：拆分姓名/工号、合并两列、格式化日期、提取括号里的内容。

> 🟢 **为什么优先教它**：`LEFT(F2,2)` 是**写死的**——万一分隔符位置变了就失效。而 Ctrl+E 理解的是"你要什么"，不是"怎么算"。这正是 AI 时代的思路转变：**告诉 Excel 你要什么，而不是告诉它怎么算**。

> 🟡 局限：它是"一次性"的，源数据变了不会自动更新。要动态更新还得用公式。

### 2.4 数据录入技巧

- **序列填充**：输入前两个值，拖拽右下角填充柄自动生成序列；按住 `Ctrl` 拖拽则强制复制
- **数据验证（下拉框）**：`数据 → 数据验证 → 允许"序列" → 来源填"花呗,借呗"`（**英文逗号**）。杜绝错别字录入

### 2.5 整理四件套

| 功能 | 入口 | 要点 |
| --- | --- | --- |
| 查找替换 | `Ctrl+F` / `Ctrl+H` | 替换时勾选「匹配整个单元格」避免误伤 |
| 排序 | `数据 → 排序` | 可设多条件（先按金额降序，再按日期升序） |
| 筛选 | `Ctrl+Shift+L` | 多个条件是「同时满足」 |
| 删除重复值 | `数据 → 删除重复值` | **真的会删行，先备份** |

### 2.6 文本连接：& 与 TEXTJOIN

```text
=B2&"-"&C2
```

`&` 连接文本，固定文本用英文双引号包起来。

批量合并一片区域用 `TEXTJOIN`（可以自动加分隔符、跳过空值）：

```text
=TEXTJOIN("、",TRUE,A2:A10)
```

### 2.7 练一练

用第 1 章的主数据：

1. 成交金额列设「货币」0 位小数，成交客户数设「数值」
2. 给成交金额加**数据条**
3. 用 **Ctrl+E** 从业务组提取省份到新列（先手动填 2 行）
4. 给"产品"列加下拉框（花呗 / 借呗）
5. 冻结首行，按成交金额降序排列
6. 筛选出「产品 = 花呗」且「成交金额 ≥ 15000」的记录

### ✅ 参考答案（做完再看）

```text
=TEXTJOIN("、",TRUE,C2:C25)
```

- 数据条：选中 E2:E25 → 条件格式 → 数据条
- Ctrl+E：在 G2 输入 `广东`、G3 输入 `广东`，然后 `Ctrl+E`，G 列应全部自动填好（广东/广西/海南）
- 筛选结果：花呗且 ≥15000 共 **5 笔**（18500、17200、16800、16500、15600），合计 **84600**
- 降序后第一行应为 **18500**

### 🤖 问 AI

> "Excel 里有一列'广东-1组'这样的文本，我想拆成省份和小组两列，有哪几种方法？分别适合什么情况？"

---

## 第 3 章 Power Query：让清洗流程可以复用

> 🟡 **本章场景**：下个月数据来了，一样脏。你不想把清洗步骤手工重做一遍。
> 🟡 **学完你能交付**：一条"换数据 → 点刷新 → 出结果"的自动化流程。
>
> 🔴 **WPS 用户注意**：Power Query 在 WPS 中不支持。请用 **Excel 2016 及以上桌面版**。

### 3.1 为什么把它放在前面

清洗本来就该在**建指标之前**。很多教程把它放在透视表之后，那是工具书的排法，不符合工作流——数据还没弄干净，算出来的指标能信吗？

PQ 的核心价值：**把清洗步骤录下来，下次一键重放**。手工清洗做一次散一次，PQ 把每步记成"菜谱"，换数据点刷新就重跑。

### 3.2 导入数据

| 来源 | 路径 |
| --- | --- |
| 表格 / 区域（最常用） | `数据 → 来自表格/区域` |
| 另一个工作簿 | `数据 → 获取数据 → 来自文件 → 从 Excel 工作簿` |
| 整个文件夹（批量合并） | `数据 → 获取数据 → 来自文件 → 从文件夹` → 合并和加载 |

> 🟡 **"从文件夹"是隐藏大招**：把 12 个月报表丢进一个文件夹，一次导入全部合并。前提是各文件**表结构一致**。
> 🔴 文件或工作表改名会导致刷新失败——**建好后尽量别挪位置**。

### 3.3 逆透视：二维表转一维表（本章重点）

这是第 0 章"要一维表"那个原则的**兑现工具**。

操作：导入数据后，**选中那几个月份列** → `转换 → 逆透视列`。

```text
业务组	1月	2月	3月          →      业务组	属性	值
广东-1组	28100	24000	10200          广东-1组	1月	28100
                                        广东-1组	2月	24000
                                        ……
```

二维表立刻变成规范的一维表，可以直接做透视分析了。

### 3.4 常用清洗操作

| 操作 | 位置 | 说明 |
| --- | --- | --- |
| 删除重复项 | `开始 → 删除行` | 按选中列判重 |
| 填充空值 | `转换 → 填充 → 向下` | 处理合并单元格留下的空 |
| 拆分列 | `转换 → 拆分列` | 按分隔符 / 字符数 / 位置 |
| 提取年月 | `转换 → 日期 → 年/月份` | 仅对真日期有效 |
| **分组依据** | `转换 → 分组依据` | 相当于在 PQ 里做透视表，清洗阶段就能出汇总 |
| 条件列 | `添加列 → 条件列` | 相当于 IF，不用写公式 |

### 3.5 两张表怎么拼

- **追加查询**（上下拼）：结构相同的表摞起来，如 1 月 + 2 月
- **合并查询**（左右拼）：相当于 VLOOKUP，按关联字段横向拼，可选内/左/右/全连接

> 🟡 **左连接最常用**：保留左表全部行，右表匹配上的补进来，没匹配上的留空。

### 3.6 加载

`开始 → 关闭并上载`（到新工作表）或 `关闭并上载至`（可选：仅创建连接 / 表 / 透视表 / 透视图）。

### 3.7 练一练：把二维表逆透视

新建工作表，录入下面这份**二维交叉表**（它就是第 1 章主数据按业务组×月份的汇总）：

| 业务组 | 1月 | 2月 | 3月 |
| --- | --- | --- | --- |
| 广东-1组 | 28100 | 24000 | 10200 |
| 广东-2组 | 31500 | 16800 | 30000 |
| 广西-1组 | 22700 | 44300 | 26000 |
| 海南-1组 | 9800 | 19000 | 24700 |

任务：

1. 导入 Power Query
2. 选中三个月份列 → **逆透视**
3. 把"属性"列改名为"月份"，"值"列改名为"成交金额"
4. 用**分组依据**按业务组汇总，验证总额
5. 加载回 Excel
6. 回去改一个源数字 → 刷新，验证流程复用

### ✅ 参考答案（做完再看）

- 逆透视后得到 **12 行**（4 个业务组 × 3 个月），三列：业务组、月份、成交金额
- 总额 **287100**，与第 1 章主数据一致 ✅（这就是交叉验证）
- 按业务组汇总：广西-1组 **93000**、广东-2组 **78300**、广东-1组 **62300**、海南-1组 **53500**
- 按月份汇总：1月 **92100**、2月 **104100**、3月 **90900**

> 🟡 **对照思考**：同样一份数据，二维表没法分析，一维表可以随意切片。这就是第 0 章那条原则的实际价值。

### 🤖 问 AI

> "我在 Power Query 里逆透视后，属性列显示的是 1月/2月/3月，怎么把它变成真正的日期？"

---

## 第 4 章 基础函数：算出第一个指标

> 🟡 **本章场景**：领导要"总业绩、平均单笔、最高最低、账期多久"。
> 🟡 **学完你能交付**：带汇总行和派生列（年月、客单价、账期）的明细表。

### 4.1 函数是什么

```text
=函数名(参数1, 参数2, ...)
```

以 `=` 开头，参数放括号里，用**英文逗号**分隔。

相比手算，函数的价值不只是快——而是**改了源数据结果自动更新**，且公式可以被别人检查。

### 4.2 聚合函数

| 函数 | 作用 | 示例 |
| --- | --- | --- |
| `SUM` | 求和 | `=SUM(E2:E25)` |
| `AVERAGE` | 平均 | `=AVERAGE(E2:E25)` |
| `COUNT` | 数**数值**单元格 | `=COUNT(D2:D25)` |
| `COUNTA` | 数**非空**单元格（含文本） | `=COUNTA(A2:A25)` |
| `MAX` / `MIN` | 最大 / 最小 | `=MAX(E2:E25)` |

> 🟡 **COUNT vs COUNTA**：数"多少条记录"用 `COUNTA`；数"多少个有效数值"用 `COUNT`。混用会少算。
> 🔴 **文本型数字不计入 SUM**：左上角有绿三角的就是它，`SUM` 会无视——**结果偏小且不报错**。按第 0 章"分列"处理。

**快捷键**：`Alt+=` 自动求和。

### 4.3 日期计算：分析师天天用

第 1 章讲了 `YEAR`/`MONTH` 取字段。但真实需求是**算日期**：

| 函数 | 作用 | 示例 |
| --- | --- | --- |
| `DATEDIF` | 算两个日期的间隔 | `=DATEDIF(A2,$I$1,"d")` 算距今天数 |
| `EDATE` | 偏移 N 个月 | `=EDATE(A2,-1)` 上月同期 |
| `EOMONTH` | 当月最后一天 | `=EOMONTH(A2,0)` 月末 |
| `TODAY` | 今天（每次打开自动更新） | `=TODAY()` |

> 🟡 **典型用法**：算账龄、算上月同期做环比、算月末做月度截止。
> 🔴 `DATEDIF` 是个"隐藏函数"——Excel 不会自动提示，但要记住：**算日期差就用它**。

### 4.4 错误处理：IFERROR

常见错误：`#DIV/0!`（除以零）、`#N/A`（找不到）、`#VALUE!`（类型不对）、`#NAME?`（函数名写错）。

```text
=ROUND(IFERROR(E2/D2,0),2)
```

> **做除法一律包上 IFERROR**——算客单价、完成率时，分母很可能是 0。
> 🟡 `#####` 不是错误，只是列宽不够，拉宽即可。

### 4.5 文本处理

| 函数 | 作用 | 业务用途 |
| --- | --- | --- |
| `LEFT` / `MID` / `RIGHT` | 截取 | 拆字符串 |
| `LEN` | 数字符数 | **校验数据**：`=LEN(B2)` 应为 3，不是就说明录错了 |
| `FIND` / `SEARCH` | 找某个字符的位置 | **让截取变动态**（见第 6 章） |
| `TEXT` | 按格式转文本 | `=TEXT(A2,"yyyy-mm")` 生成"2026-01" |
| `SUBSTITUTE` | 替换指定文本 | 统一分隔符、去掉脏字符 |

### 4.6 练一练

用主数据（表头第 1 行，数据第 2–25 行）：

1. G 列算成交年份、H 列算成交月份
2. I 列算客单价，用 `IFERROR` 兜底并保留 2 位小数
3. 在第 27 行用 SUM / AVERAGE / MAX / MIN 汇总成交金额
4. 在 J1 输入基准日期 `2026/4/1`，J 列用 `DATEDIF` 算每笔成交距基准日的天数
5. K 列用 `EOMONTH` 算出每笔成交所在月的月末
6. L 列用 `LEN` 检查工号位数是否都是 3

### ✅ 参考答案（做完再看）

```text
=YEAR(A2)
```

```text
=MONTH(A2)
```

```text
=ROUND(IFERROR(E2/D2,0),2)
```

```text
=DATEDIF(A2,$J$1,"d")
```

```text
=EOMONTH(A2,0)
```

```text
=LEN(B2)
```

预期结果：

| 项目 | 结果 |
| --- | --- |
| 成交金额总额 | 287100 |
| 平均 | 11962 |
| 最高 / 最低 | 18500 / 6800 |
| 总客户数 | 540 |
| 整体客单价 | 531.67（287100 ÷ 540） |
| 客单价区间 | 441.18 ~ 600.00 |
| 第 1 行（1/5）距 4/1 | 86 天 |
| 第 1 行月末 | 2026/1/31 |

> 🟡 **交叉验证**：花呗 170500 + 借呗 116600 = 287100，与总额吻合才算对。

### 🤖 问 AI

> "帮我逐段解释这个 Excel 公式在干什么：=ROUND(IFERROR(E2/D2,0),2)"

---

## 第 5 章 常用函数：按条件算

> 🟡 **本章场景**：领导要"各产品业绩""达标的有几笔"，还会点名问"003 卖了多少"。

### 5.1 引用方式：三种写法

往下拖、往右拖时，引用会不会跟着跑——这是唯一区别。

| 类型 | 写法 | 行为 |
| --- | --- | --- |
| 相对引用 | `E2` | 跟着跑（下拖变 `E3`） |
| 绝对引用 | `$E$2` | 完全不动 |
| 混合引用 | `$E2` / `E$2` | 只锁行或只锁列 |

> 🟡 编辑公式时选中区域引用，按 **F4** 循环切换四种状态。
> 🔴 **查找区域、排名区域必须绝对引用**。漏了 `$`，下拉后区域下移，**不报错但结果全错**。

### 5.2 条件判断：IF 与 IFS

```text
=IF(E2>=15000,"高",IF(E2>=10000,"中","低"))
```

嵌套多了可读性很差，这时用 **`IFS`**——按顺序判断，写法扁平：

```text
=IFS(E2>=15000,"高",E2>=10000,"中",TRUE,"低")
```

> 🔴 两个坑：① 比较符号必须半角 `>=`，**不能写 `≥`**（Excel 不认，直接弹"公式有问题"）；② `IFS` 最后要加 `TRUE` 兜底，否则都不满足时返回 `#N/A`。
> 🟡 文本结果要加英文双引号，漏了报 `#NAME?`。

### 5.3 SUBTOTAL：筛选后求和的唯一正确做法

**这是本章最重要的一个坑。**

你筛选了"只看花呗"，然后写 `=SUM(E2:E25)`——得到的**仍是全部 24 行的总额**，因为 `SUM` 会把隐藏行也算进去。

正确做法是用 `SUBTOTAL`：

```text
=SUBTOTAL(109,E2:E25)
```

`109` 代表"求和且**忽略隐藏行**"。筛选后它只算看得见的行。

> 🔴 **只要你习惯筛选着看数，就该用 SUBTOTAL 而不是 SUM**。这个错误不报错，只是数字不对——你基于错误数字做的判断全是错的。
> 其他常用编号：`101` 平均值、`104` 最大值、`103` 非空计数。

### 5.4 单条件统计：SUMIF / COUNTIF

SUMIF 管"加起来多少"，COUNTIF 管"**有多少条**"。

```text
=SUMIF(C2:C25,"花呗",E2:E25)
```

```text
=COUNTIF(E2:E25,">=15000")
```

写之前想清楚三件事：按哪列筛（条件区域）、筛什么（条件）、加哪列（求和区域）。

> 🔴 条件里的比较符号要带引号且半角：`">=15000"`；条件区域与求和区域**行数必须一致**。

### 5.5 数据匹配：XLOOKUP 与 VLOOKUP

> 🟢 **Excel 2021 / 365 / 2024 请直接用 XLOOKUP**。VLOOKUP 有三个先天缺陷：只能从左往右查、列序号要数、默认模糊匹配。

```text
=XLOOKUP(P2,B2:B25,E2:E25,"未找到",0)
```

读法：`XLOOKUP(找什么, 在哪列找, 返回哪列, 找不到显示什么, 匹配模式)`。**不关心方向**，改区域即可正查反查。

VLOOKUP 只在维护遗留表格时需要：

```text
=VLOOKUP(P2,$B$2:$F$25,4,FALSE)
```

三条规矩：查找列必须在区域最左、第 3 参数要数出来（B=1,C=2,D=3,E=4,F=5）、第 4 参数务必 `FALSE`。

> 🔴 返回 `#N/A` 绝大多数是**类型不一致**（查找值是文本 `"001"`，表里是数字 `1`）；也可能是肉眼看不见的尾部空格，用 `TRIM` 清理。

### 5.6 练一练

1. 用 `IFS` 给 M 列打等级：≥15000 高、≥10000 中、其余低
2. 用 SUMIF 统计花呗、借呗、工号 003 的成交总额
3. 用 COUNTIF 统计 ≥15000 的笔数
4. **对比实验**：写 `=SUM(E2:E25)` 和 `=SUBTOTAL(109,E2:E25)`，然后筛选"只看花呗"，看两个数各是多少
5. 用 XLOOKUP 建查询区：P2 输入工号，查出对应产品和金额

### ✅ 参考答案（做完再看）

```text
=IFS(E2>=15000,"高",E2>=10000,"中",TRUE,"低")
```

```text
=SUMIF(C2:C25,"花呗",E2:E25)
```

```text
=SUMIF(B2:B25,"003",E2:E25)
```

```text
=COUNTIF(E2:E25,">=15000")
```

```text
=SUBTOTAL(109,E2:E25)
```

```text
=XLOOKUP($P$2,B2:B25,E2:E25,"未找到",0)
```

预期结果：

| 项目 | 结果 |
| --- | --- |
| 花呗总额 | 170500 |
| 借呗总额 | 116600 |
| 工号 003 总额 | 84400 |
| ≥15000 笔数 | 5 |
| 不筛选时 SUM / SUBTOTAL | 都是 287100 |
| **筛选"花呗"后** | SUM 仍 **287100**（错）｜SUBTOTAL 变 **170500**（对） |
| 等级分布 | 高 5 笔、中 12 笔、低 7 笔 |

> 🔴 **务必亲手做第 4 题**。看过一次这个差异，你以后就不会再犯。

### 🤖 问 AI

> "Excel 里筛选之后 SUM 的结果没有跟着变，为什么？该怎么改？"

---

## 第 6 章 高级函数：排名、分布与动态截取

> 🟡 **本章场景**："业绩前 3 名是谁""客单价中位数多少""把'广东-1组'拆成省份和小组"。

### 6.1 排名与分位数

| 函数 | 作用 | 示例 |
| --- | --- | --- |
| `RANK` | 排名 | `=RANK(E2,E$2:E$25)` 区域要绝对引用 |
| `LARGE` / `SMALL` | 第 N 大 / 第 N 小 | `=LARGE(E$2:E$25,1)` |
| `MEDIAN` | 中位数 | `=MEDIAN(E2:E25)` |
| `PERCENTILE` | 任意分位数 | `=PERCENTILE(E$2:E$25,0.9)` 算 P90 |
| `QUARTILE` | 四分位数 | `=QUARTILE(E$2:E$25,1)` 算 Q1 |

> 🟡 **`QUARTILE` / `PERCENTILE` 默认都是 INC（含端点）写法**。Excel 里还有一套 `QUARTILE.EXC` / `PERCENTILE.EXC`（不含端点），两组函数算法不同、结果也不同。
> 本数据集 n=24：Q1 用 INC 是 **9500**，用 EXC 是 **9300**；Q3 用 INC 是 **13900**，用 EXC 是 **14500**。
> **日常用默认的 INC 即可**，但和同事对数时务必确认口径一致 —— 这是"两个人都没算错，数字却对不上"最常见的来源。

> 🟡 **为什么中位数比平均数常用**：平均会被极值拉偏。有 1 笔异常大的单，平均就虚高了。**看分布先看中位数，再看 P90**，比只看平均数靠谱得多。

### 6.2 FIND：让文本截取变动态

第 2 章说过 `=LEFT(F2,2)` 是**写死的**——万一出现"内蒙古-1组"（3 个字），就截错了。

正确做法是先找到分隔符位置，再截取：

```text
=LEFT(F2,FIND("-",F2)-1)
```

```text
=MID(F2,FIND("-",F2)+1,99)
```

前者取 `-` 左边（省份），后者取 `-` 右边（小组）。**不管省份是 2 个字还是 3 个字都成立**。

> 🟢 这是从"能算出结果"到"算得稳健"的分界线。写公式时多问一句：**换个数据它还成立吗？**

### 6.3 SUMPRODUCT：不用辅助列的多条件计算

```text
=SUMPRODUCT((C2:C25="花呗")*(E2:E25))
```

原理：`(C2:C25="花呗")` 生成一串 TRUE/FALSE，乘上金额后 TRUE 当 1、FALSE 当 0，再求和。

它比 SUMIFS 更灵活——能做加权、跨列判断。但**数据量大时比 SUMIFS 慢**，日常优先用 SUMIFS。

### 6.4 多条件求和：SUMIFS

```text
=SUMIFS(E2:E25,C2:C25,"花呗",E2:E25,">=15000")
```

> 🔴 **和 SUMIF 参数顺序不一样**——`SUMIFS` 的**求和区域在最前面**，最容易记混。所有条件同时满足。

### 6.5 动态数组：一个公式返回一片结果

```text
=FILTER(A2:F25,C2:C25="花呗")
```

```text
=SORT(A2:F25,5,-1)
```

```text
=UNIQUE(F2:F25)
```

分别是筛出花呗全部记录、按第 5 列降序、列出不重复的业务组。**结果随源数据自动更新**，等于做了个"活"的查询表。

### 6.6 练一练

1. G 列用 `RANK` 给成交金额排名
2. 用 `LARGE` 取出第 1、2、3 名的金额
3. 算出成交金额的中位数、Q1、P90
4. 用 `FIND` + `LEFT`/`MID` 把业务组拆成"省份"和"小组"两列
5. 用 `FILTER` 一次性列出所有"借呗"的记录
6. 用 SUMIFS 统计：花呗且 ≥15000 的总额

### ✅ 参考答案（做完再看）

```text
=RANK(E2,E$2:E$25)
```

```text
=LARGE(E$2:E$25,1)
```

```text
=MEDIAN(E2:E25)
```

```text
=QUARTILE(E2:E25,1)
```

```text
=PERCENTILE(E$2:E$25,0.9)
```

```text
=LEFT(F2,FIND("-",F2)-1)
```

```text
=MID(F2,FIND("-",F2)+1,99)
```

```text
=FILTER(A2:F25,C2:C25="借呗")
```

```text
=SUMIFS(E2:E25,C2:C25,"花呗",E2:E25,">=15000")
```

预期结果：

| 项目 | 结果 |
| --- | --- |
| Top 3 金额 | 18500、17200、16800 |
| 第 1 名的那行 | 2026/2/10 工号 003 广西-1组 |
| 中位数 | 11350 |
| Q1 / Q3 | 9500 / 13900 |
| P90 | 16710 |
| 拆出的省份 | 广东、广西、海南（3 个不重复值） |
| 花呗且 ≥15000 | 84600（5 笔） |
| 借呗记录数 | 12 条 |

> 🟡 **观察**：均值 11962 > 中位数 11350，说明数据是**右偏**的——有几笔大额把平均拉高了。这就是"只看平均数会误判"的实证。

### 🤖 问 AI

> "我想在 Excel 里算 P90 分位数，并且排除掉异常值，应该用什么公式？"

---

## 第 7 章 数据透视表：分析师的第一武器

> 🟡 **为什么这章篇幅最重**：如果只能留一个 Excel 技能，留透视表。它不用写公式，靠拖字段就能把数据翻来覆去地看——**分析师的日常，八成是在拖透视表**。

### 7.1 前提：数据源必须规范

> 🔴 **透视表失败，九成是数据源的锅**。开始前确认：表头只有一行且不重名、**没有合并单元格**、没有空行空列切断数据、一列只放一种数据。
> 🟢 建议先 `Ctrl+T` 转智能表再建透视表，后续新增数据刷新即可纳入。

### 7.2 创建

1. 选中数据区（含表头）
2. `插入 → 数据透视表` → 选「**新工作表**」→ 确定
3. 右侧字段窗格四个区域：**行**（分类）、**列**（交叉）、**值**（算什么）、**筛选**（全局过滤）

| 想看什么 | 怎么拖 |
| --- | --- |
| 各工号业绩 | 销售工号 → 行；成交金额 → 值 |
| 产品 × 业务组交叉 | 产品 → 行；业务组 → 列；成交金额 → 值 |
| 只看某个月 | 成交月份 → 筛选 |

### 7.3 值的两种玩法

- **值汇总方式**：右键值字段 → 值字段设置 → 改成求和 / 计数 / 平均 / 最大 / 最小
- **值显示方式**（做二次计算，分析全靠它）：

| 显示方式 | 用途 |
| --- | --- |
| **总计的百分比** | 看占比、看结构 |
| **列汇总的百分比** | 看每组内部的构成 |
| **差异** | 与基准值的差值（如"比 001 高多少"） |
| **差异百分比** | 增长率 |
| **按某一字段汇总** | 累计值（Running Total） |

> 🟢 **"值显示方式"是透视表被低估的功能**。很多人只会拖求和，其实占比、增长、累计都在右键菜单里，不用写任何公式。

### 7.4 日期分组：把流水账变成趋势

右键透视表里的日期字段 → **组合** → 可选日/月/季/年（**按住 Ctrl 可多选**）。

选「月」立刻得到按月汇总；同时选「年」和「月」则按年月分层。

> 🟢 这是做趋势分析最快的方法，**比写公式快得多**。
> 🔴 前提：那一列必须是**真正的日期格式**，文本日期无法分组（先按第 0 章转换）。

### 7.5 计算字段：在透视表里造新指标

`数据透视表分析 → 计算 → 字段、项目和集 → 计算字段`

例如新增"平均客单价" = 成交金额 ÷ 成交客户数：

```text
= 成交金额 / 成交客户数
```

> 🔴 **注意**：计算字段是**先各自求和再相除**（总额÷总客户数），不是"每行客单价的平均"。想算后者，应该在源数据里先算出客单价列，再拖进透视表求平均。**这两种算法结果不同，别搞混。**

### 7.6 切片器：给报表加上按钮

`数据透视表分析 → 插入切片器` → 勾选字段（产品 / 业务组）。

点一下就筛选，多个切片器**联动**。还可以 `切片器工具 → 报表连接`，让**多个透视表同步响应**同一个切片器——做看板就靠这个。

### 7.7 多表透视

`插入 → 数据透视表` 时勾选「**将此数据添加到数据模型**」，就能同时用多张表的字段（详见第 9 章 Power Pivot，那里是完整版）。

### 7.8 刷新与出图

- **刷新**：源数据改了 → 右键 → 刷新（或 `Alt+F5`）
- **透视图**：`数据透视表分析 → 数据透视图`，图表会随切片器同步变化

### 7.9 练一练

基于主数据建透视表：

1. 按业务组看成交金额总和，再加一个"总计的百分比"
2. 产品（行）× 业务组（列）交叉看金额
3. 把日期按「月」组合，看月度趋势
4. 插入"产品"切片器，点选花呗看变化
5. 加计算字段"平均客单价"，观察它和"每行客单价再平均"是否一样
6. 基于 1 插一个柱状透视图
7. 改源数据某行的金额 → 刷新，验证联动

### ✅ 参考答案（做完再看）

| 维度 | 结果 |
| --- | --- |
| 广西-1组 | 93000（32.4%） |
| 广东-2组 | 78300（27.3%） |
| 广东-1组 | 62300（21.7%） |
| 海南-1组 | 53500（18.6%） |
| 月度趋势 | 1月 92100 → 2月 104100 → 3月 90900 |
| 合计校验 | 93000+78300+62300+53500 = **287100** ✅ |

> 🟡 **自检习惯**：透视表的合计**必须等于**第 4 章 `SUM` 的结果。对不上，一定是混进了文本型数字或空行。

### 🤖 问 AI

> "Excel 透视表里'值显示方式'有哪些选项？分别用在什么分析场景？"

---

## 第 8 章 分析方法：这才是"分析"

> 🔵 **本章是全篇的分水岭**。前面七章教你把数算出来，这一章教你**回答"所以呢"**。
> 会做表的人算出"3月成交 90900"；会分析的人说出"**3月下滑 12.7%，主要是广西-1组的花呗少了 20900，建议重点排查该区域**"。

### 8.1 分析只有三个动作

所有分析，拆到底就是这三件事：

| 动作 | 在回答什么 | 典型问法 |
| --- | --- | --- |
| **对比** | 好还是差？ | 比目标、比上月、比去年同期、比同行 |
| **拆解** | 差在哪？ | 按产品拆、按区域拆、按客户拆、按时间拆 |
| **归因** | 为什么？ | 找到那个主要拖累项，追问原因 |

> 🔴 **没有对比的数字没有意义**。"3月成交 90900"——是好是坏？不知道。但"比 2 月跌了 12.7%"就立刻有了判断。

### 8.2 对比：同比、环比、基准

| 概念 | 定义 | 公式 |
| --- | --- | --- |
| **环比** | 和上一个相邻周期比 | （本期 − 上期）÷ 上期 |
| **同比** | 和去年同一周期比 | （本期 − 去年同期）÷ 去年同期 |
| **比目标** | 和预算 / KPI 比 | 实际 ÷ 目标 − 1 |

> 🟡 **什么时候用哪个**：看**短期趋势和季节性**用同比（排除季节因素）；看**最近变化**用环比。电商大促月用环比会被误导——去年也是大促。

在透视表里做环比：值显示方式 → **差异百分比**，基本字段选"月份"，基本项选"（上一个）"。

### 8.3 拆解：把总量拆成部分

总量变化 = 各部分变化之和。这是拆解的数学基础，也是为什么它能定位问题。

**实操套路**：在透视表里把维度一层层拖进去——先看业务组，再在产品维度下钻，必要时再拆到工号。

### 8.4 归因案例（本章重点练习）

> **问题**：3 月成交额 90900，比 2 月的 104100 **下降 13200（-12.7%）**。为什么？

**第一步：按业务组拆**

| 业务组 | 2月 | 3月 | 变化 |
| --- | --- | --- | --- |
| 广西-1组 | 44300 | 26000 | **−18300** |
| 广东-1组 | 24000 | 10200 | **−13800** |
| 广东-2组 | 16800 | 30000 | +13200 |
| 海南-1组 | 19000 | 24700 | +5700 |
| 合计 | 104100 | 90900 | −13200 |

**第二步：继续下钻到「业务组 × 产品」**

| 组合 | 2月 | 3月 | 变化 |
| --- | --- | --- | --- |
| **广西-1组 / 花呗** | 35700 | 14800 | **−20900** |
| 广东-1组 / 借呗 | 10800 | 0 | −10800 |
| 海南-1组 / 借呗 | 19000 | 8200 | −10800 |
| 广东-2组 / 花呗 | 16800 | 13600 | −3200 |
| 广东-1组 / 花呗 | 13200 | 10200 | −3000 |
| 广西-1组 / 借呗 | 8600 | 11200 | +2600 |
| 广东-2组 / 借呗 | 0 | 16400 | +16400 |
| 海南-1组 / 花呗 | 0 | 16500 | +16500 |

**第三步：下结论**

> 3 月下滑 13200，**主因是广西-1组的花呗产品缩水 20900**（从 35700 跌到 14800）。
> 虽然海南-1组的花呗（+16500）和广东-2组的借呗（+16400）有明显增量，但不足以弥补缺口。
> **建议**：重点排查广西-1组花呗业务——是客户需求变化、竞品进入，还是销售人员变动？

> 🟢 **这就是分析**。同样一份数据，会做表的人只报出"跌了 12.7%"，会分析的人能指出是谁拖的、该查什么。
> 注意最后一步：**从"是什么"推进到"该做什么"**。没有建议的分析报告价值减半。

### 8.5 帕累托分析（80/20）

**用法**：把贡献从大到小排序，算累计占比，找出贡献 80% 的那少数几项——**抓重点就靠它**。

按「业务组 × 产品」降序：

| 排名 | 组合 | 金额 | 占比 | 累计 |
| --- | --- | --- | --- | --- |
| 1 | 广东-1组/花呗 | 51500 | 17.9% | 17.9% |
| 2 | 广西-1组/花呗 | 50500 | 17.6% | 35.5% |
| 3 | 广西-1组/借呗 | 42500 | 14.8% | 50.3% |
| 4 | 广东-2组/花呗 | 42200 | 14.7% | 65.0% |
| 5 | 广东-2组/借呗 | 36100 | 12.6% | 77.6% |
| 6 | 海南-1组/借呗 | 27200 | 9.5% | **87.1%** ← |
| 7 | 海南-1组/花呗 | 26300 | 9.2% | 96.2% |
| 8 | 广东-1组/借呗 | 10800 | 3.8% | 100% |

> 结论：**前 5 个组合（共 8 个中的 5 个）贡献了 77.6% 的业绩**；末位的"广东-1组/借呗"只占 3.8%，投入产出比低，可以考虑收缩。

### 8.6 分布分析：别只看平均数

| 指标 | 值 | 说明 |
| --- | --- | --- |
| 均值 | 11962 | 会被极值拉高 |
| 中位数 | 11350 | 典型水平 |
| Q1 | 9500 | 后 25% 低于此 |
| Q3 | 13900 | 前 25% 高于此 |

> 🔴 **均值 > 中位数，说明数据右偏**——有几笔大额把平均拉高了。此时汇报"平均每单 11962"会误导，应该说"中位数 11350，但前 25% 的单子超过 13900"。

**分段统计**（看分布形状）：用 `COUNTIFS` 数各区间有多少笔：

```text
=COUNTIFS(E$2:E$25,">=10000",E$2:E$25,"<15000")
```

### 8.7 两个常用模型（了解）

**RFM 客户分层**——三个维度给客户打分，用于分层运营：

| 维度 | 含义 | 算法 |
| --- | --- | --- |
| R 最近一次消费 | 越近越有价值 | 今天 − 最近成交日 |
| F 消费频率 | 次数越多越忠诚 | 成交笔数 |
| M 消费金额 | 花得越多越重要 | 累计金额 |

三者各分高低两档，组合出 8 类客户（重要价值客户、流失客户等），对应不同运营策略。

**Cohort 留存矩阵**——按"首次成交月份"把客户分组，看他们在后续各月是否还在买。横轴是"第 N 个月"，纵轴是"哪一批客户"，格子是留存率。它能回答："我们留住客户的能力是在变好还是变差？"

> 🟡 这两个模型本身不难，难在**有人告诉你该用它们**。先把对比/拆解/归因练熟，再上模型。

### 8.8 练一练

用主数据完成上面的归因分析：

1. 建透视表：业务组（行）+ 月份（列），看 2 月到 3 月各组的涨跌
2. 再加"产品"到行（在业务组下方），下钻到「业务组 × 产品」
3. 找出变化最大的负向项和正向项
4. 用"值显示方式 → 总计的百分比"做帕累托，找出贡献 80% 的组合
5. 用 `MEDIAN`、`QUARTILE` 算分布，判断均值是否可信
6. **写一段结论**：3 月为什么下滑？该做什么？

### ✅ 参考答案（做完再看）

- 最大负向：**广西-1组/花呗 −20900**
- 最大正向：海南-1组/花呗 +16500、广东-2组/借呗 +16400
- 帕累托 80% 线：累计到第 **6** 个组合（87.1%）
- 分布：中位数 11350 < 均值 11962，右偏，**均值不可信**
- 结论要点：主因是广西-1组花呗缩水；建议排查该区域花呗业务

### 🤖 问 AI

> "我有一份销售明细，3月环比下滑12.7%。请给我一个系统的拆解分析框架，告诉我应该按哪些维度拆、每一步要回答什么问题。"

---

## 第 9 章 Power Pivot：多表建模与 DAX

> 🟡 **本章场景**：销售表和员工表是分开的，你想知道"管理人员 vs 普通员工的业绩差异"。
> 🔴 **WPS 用户**：Power Pivot 不支持，请用 Excel 桌面版。

### 9.1 它解决什么问题

普通透视表只能基于一张表。想跨表分析，传统做法是先用 VLOOKUP 把字段拼过来——数据量大就卡，字段一变要重做。

Power Pivot 的做法：**把多张表装进一个"数据模型"，建立关系，之后随便跨表拖字段**。支持百万级数据和 DAX 公式。

启用：`文件 → 选项 → 加载项 → COM 加载项` → 勾选 `Microsoft Power Pivot for Excel`。

### 9.2 星型模型：建模的理论基础

这是很多人建关系时**知其然不知其所以然**的地方。标准结构是"一星"：

```
               ┌──────────┐
               │ 员工维度表 │  （工号、姓名、职务、区域）
               └────┬─────┘
                    │ 工号
        ┌───────────┼───────────┐
┌───────┴──┐              ┌─────┴──────┐
│ 日期维度表 │──────────────│  销售事实表  │
│（日期、月、季）│   日期      │（日期、工号、 │
└───────────┘              │ 产品、金额…）│
                           └──────┬─────┘
                                  │ 产品
                           ┌──────┴─────┐
                           │ 产品维度表  │
                           └────────────┘
```

- **事实表**：记录"发生了什么"，数字多、行数多（如销售明细）
- **维度表**：记录"谁、什么、哪里、何时"，行数少、用于切分（如员工表、产品表、日期表）

> 🟢 **为什么这么建**：维度表可以**被多个事实表共用**，而且筛选维度表的字段时，事实表会自动跟着过滤。这就是"建关系"的意义——不是随便连两列，而是让筛选能顺着关系**流动**。

### 9.3 建关系

1. 两张表分别 `Power Pivot → 添加到数据模型`
2. `关系视图` 里拖动关联字段（销售表[销售工号] → 员工表[销售工号]）
3. 关系建立后，透视表就能跨表拖字段了

### 9.4 计算列 vs 度量值（最关键的区别）

| | 计算列 | 度量值 |
| --- | --- | --- |
| 算什么 | **每一行**自己的值 | 对**整个模型**的聚合 |
| 何时算 | 建好就固定，存进表里 | 用时才算，**随筛选动态变化** |
| 占空间 | 占内存 | 几乎不占 |
| 用途 | 客单价（每行一条） | 总业绩（汇总） |

> 🟡 **判断口诀**：要"这一行的属性"→ 计算列；要"汇总指标"→ 度量值。

### 9.5 DAX 入门

DAX 用 `表名[列名]` 的写法。

**基础度量值**

```text
=SUM(销售表[成交金额])
```

```text
=AVERAGE(销售表[成交金额])
```

**CALCULATE：带条件的聚合（核心函数）**

```text
=CALCULATE(表达式, 筛选条件1, 筛选条件2, ...)
```

例：只算管理人员的平均业绩

```text
=CALCULATE(AVERAGE(销售表[成交金额]), 员工表[职务类别]="管理人员")
```

> 🔴 第一个参数是要算什么，后面全是筛选条件，可以叠加多个。

**计算列示例**

```text
=销售表[成交金额]/销售表[成交客户数]
```

**时间智能**（做累计、同比的正规做法）

```text
=TOTALYTD(SUM(销售表[成交金额]), 日期表[日期])
```

```text
=CALCULATE(SUM(销售表[成交金额]), DATEADD(日期表[日期],-1,YEAR))
```

后者就是**同比**（去年同一时期）。

> 🟡 时间智能函数需要一张**独立的日期维度表**才能正常工作——这又回到 9.2 的星型模型，理论和工具是配套的。

### 9.6 练一练

1. 新建"员工信息表"，录入下面数据
2. 把主数据（销售表）和员工表都加入数据模型
3. 用「销售工号」建关系
4. 建度量值：总成交金额、管理人员平均业绩
5. 建计算列：客单价
6. 基于数据模型建透视表，按"区域""职务类别"看总成交金额

**员工信息表**

| 销售工号 | 姓名 | 职务类别 | 区域 |
| --- | --- | --- | --- |
| 001 | 张三 | 普通员工 | 广东 |
| 002 | 李四 | 普通员工 | 广东 |
| 003 | 王五 | 管理人员 | 广西 |
| 004 | 赵六 | 普通员工 | 海南 |

### ✅ 参考答案（做完再看）

```text
=SUM(销售表[成交金额])
```

```text
=CALCULATE(AVERAGE(销售表[成交金额]),员工表[职务类别]="管理人员")
```

```text
=销售表[成交金额]/销售表[成交客户数]
```

预期结果：

| 项目 | 结果 |
| --- | --- |
| 总成交金额 | 287100 |
| 管理人员（仅 003）业绩 | 84400 |
| 管理人员平均单笔 | 14066.67（84400 ÷ 6 笔） |
| 普通员工合计 | 202700（001+002+004） |
| 按区域 | 广东 149200、广西 84400、海南 53500 |

> 🟡 **口径提醒**：这里的"区域"是按 **工号 → 维度表** 映射得到的（001/002 属广东、003 属广西、004 属海南）；第 3 章交叉表里的"广东"是把 **广东-1组 + 广东-2组** 合并后的口径。第 1 章已说明**工号和业务组并非一一对应**（001 同时在广东-1组和广东-2组），所以两种口径下广东分别是 **149200** 和 **140600**，都对，但不要混用。

> 切换切片器时**度量值实时变化、计算列不变**——亲手试一次你就懂了。

### 🤖 问 AI

> "解释 DAX 里 CALCULATE 的作用，以及筛选上下文是怎么被它改变的。用新手能听懂的方式说。"

---

## 第 10 章 可视化与表达：让人一眼看懂

> 🟡 **本章场景**：数字都算对了，但领导看了三页表还是问"所以呢"。

### 10.1 图表怎么选

选图的唯一依据是**你想表达什么关系**：

| 要表达 | 用什么图 | 说明 |
| --- | --- | --- |
| **比较**大小 | 柱状图 / 条形图 | 类别多于 8 个时用条形图（横向好读） |
| **趋势**变化 | 折线图 | 时间趋势首选 |
| **占比** | 饼图 / 环形图 | **类别不超过 5 个**才用；超过就用柱状图 |
| **分布** | 直方图 / 箱线图 | 看数据集中在哪、有没有异常 |
| **两个变量的关系** | 散点图 | 看相关性 |
| **两个量级差异大的指标** | 组合图（双轴） | 如"金额+增长率" |

> 🔴 **最常犯的错**：用饼图表达趋势、用折线图表达类别比较。**折线暗示连续，柱形暗示离散**。

### 10.2 少即是多：去掉图表垃圾

- 删掉默认的网格线、不必要的图例、3D 效果
- 不用渐变色，用单色或同色系
- **直接给数据系列标数字**，省得读者去对坐标轴
- 标题写**结论**，不要写"成交金额趋势"，要写"**3月成交环比下滑12.7%**"

> 🟢 **标题写结论，这是最便宜也最有效的一招**。同一个图，标题从"成交金额趋势"改成"3月跌12.7%，主因是广西-1组"，读者Get信息的速度差好几倍。

### 10.3 做看板的基本布局

一个能用的分析看板通常是这个结构：

```
┌─────────────────────────────────┐
│  核心指标卡（总额、环比、达标率）      │  ← 一眼看结论
├──────────────┬──────────────────┤
│  趋势图（按月）  │  拆解图（按维度）    │  ← 看变化和构成
├──────────────┴──────────────────┤
│  明细表（可筛选）                    │  ← 留给人查细节
└─────────────────────────────────┘
         ↑ 顶部放切片器
```

### 10.4 结论先行：怎么汇报

分析报告的结构，推荐用 **SCQA**：

- **S（情境）**：大家都知道的背景
- **C（冲突）**：出现了什么问题
- **Q（问题）**：我们要回答什么
- **A（答案）**：你的结论

**示例**：

> **S**：一季度销售额 287100，整体达标。
> **C**：但 3 月环比下滑 12.7%（90900 vs 104100）。
> **Q**：下滑主要由什么造成？
> **A**：主因是广西-1组的花呗产品缩水 20900（35700 → 14800），占全部降幅的 158%。海南、广东-2组的新增不足以弥补。**建议**：本周内排查广西-1组花呗的客户流失情况，并评估是否需要在该区域补强人手。

> 🟢 注意最后那句**建议**。分析报告的价值 = 结论 × 可行动性。只报数字不给建议，等于把问题又扔回给了领导。

### 10.5 交付前必做的三项校验

| 校验 | 怎么做 |
| --- | --- |
| **合计校验** | 透视表合计是否等于 `SUM`？对不上就是有脏数据 |
| **交叉校验** | 换个方法再算一遍（如 SUMIF 的结果 vs 透视表） |
| **抽样核对** | 随机挑一条记录，手工验一遍它的每个指标 |

> 🔴 **数字错误是分析师最致命的失误**。它不像公式报错那样会提醒你——它静静地躺在那里，等人在会上引用它。

### 🤖 问 AI

> "我要汇报一份销售分析，结论是3月下滑主要因某区域某产品。帮我按 SCQA 结构组织成 200 字的汇报要点，并给出可执行的建议。"

---

## 第 11 章 AI 时代：怎么用它，以及别信它

> 🔵 **这一章会改变你学 Excel 的方式**。不是加个工具，是**学什么、怎么学**都要调整。

### 11.1 AI 让什么贬值，让什么升值

| 在贬值 ↓ | 在升值 ↑ |
| --- | --- |
| 记函数语法、记参数顺序 | 知道**该用什么**函数 |
| 记菜单路径 | 能**判断结果对不对** |
| 手工重复操作 | **定义问题**的能力 |
| 背快捷键 | 理解业务、讲清结论 |

> 以前背 `SUMIFS` 参数顺序是硬技能，现在问 AI 三秒就有。**花大量时间记语法，性价比正在快速下降。**

### 11.2 AI 擅长什么

- **写公式**：描述需求，它直接给公式（本篇每章末尾的「问 AI」都是这么用的）
- **解释公式**：贴一段看不懂的公式，让它逐段讲
- **排查报错**：把错误提示和公式一起贴过去
- **给分析思路**："我想分析 X，该按哪些维度拆"
- **写 DAX / M 代码**：这两门语言语法别扭，AI 特别有用

### 11.3 AI 不擅长什么（重要）

> 🔴 **AI 不知道你的数据长什么样，不知道你的业务口径，也不为结果负责。**

具体来说：

1. **它可能给"语法正确、逻辑错误"的公式** —— 比如条件区域和求和区域行数不一致，或者该用 `SUBTOTAL` 却给了 `SUM`。**它不报错，只是悄悄地算错。**
2. **它会编造不存在的函数** —— 尤其是冷门函数和新版本特性
3. **它不知道你的业务口径** —— "成交额"在你的公司是指含税还是不含税？它不知道

> 🔴 **"AI 给的公式跑通了" ≠ "算对了"。** 这点必须刻在脑子里。养成盲信的习惯，在数据工作里是灾难。

### 11.4 怎么用：一套流程

1. **描述清楚你要什么**——带上数据长什么样、想要什么结果
2. **让它给公式**
3. **必须验证**（三步）：
   - 在小范围数据上跑一遍
   - 用另一种方法交叉算一次（如 AI 给了 SUMIFS，你用透视表对一遍）
   - 挑一条记录手工核对
4. **让它解释**——确认你理解它给的每一段，不理解就继续问

**好的提问方式**（对比）：

```
❌ 差：帮我写个公式算销售
✅ 好：A列是成交日期，C列是产品（值只有"花呗""借呗"），
       E列是成交金额，数据在第2到25行。
       我要算"花呗且金额>=15000"的总额，请给我 Excel 公式，
       并说明每个参数的作用。
```

> 🟢 **给足上下文，输出质量差好几倍**。说清列名、数据范围、期望结果，AI 才不会瞎猜。

### 11.5 Excel 内置的 AI 功能

| 功能 | 怎么用 | 说明 |
| --- | --- | --- |
| **Flash Fill** | `Ctrl+E` | 手动填 2 行示例，自动提取/合并剩余（第 2 章） |
| **Copilot** | 功能区按钮 | 365 新版可用，用自然语言生成公式和分析（需订阅） |
| **Python in Excel** | `=PY()` | 365 逐步开放，能在单元格里跑 Python |
| **智能查找** | 右键 → 智能查找 | 联网查概念 |

> 🟡 **Flash Fill 的价值被严重低估**。它代表新范式：**告诉 Excel 你要什么，而不是告诉它怎么算**。日常的文本拆分合并，它比写公式快得多。

### 11.6 AI 时代，什么能力更值钱

1. **提问能力** —— 能把模糊的业务问题拆成清晰的数据问题
2. **校验能力** —— 能判断一个数字可不可信
3. **业务理解** —— 知道"成交额下降"背后可能有哪些真实原因
4. **讲结论** —— 把数字变成决策

> 🟢 这四项恰好是本篇第 8、10 章在练的东西。**当写公式变得几乎零成本，稀缺的就不再是"能把数算出来"，而是"知道该算什么、算出来说明什么"。**

### 🤖 问 AI

> "我准备用 AI 辅助做 Excel 数据分析。请帮我列一份'AI 输出校验清单'——拿到 AI 给的公式后，应该从哪几个角度验证它是对的。"

---

## 第 12 章 综合实战：独立完成一份分析报告

> 🟢 **这是毕业设计**。前面所有章节的知识，在这里串成一条线走一遍。做完这一章，你手上就有**能放进作品集的东西**了。

### 12.1 项目需求

> 你是某消费金融公司的销售分析师。领导给你一份刚导出的 1 月销售数据（很脏），要求：
> **"整理一下，告诉我 1 月卖得怎么样，有什么问题，下个月该注意什么。"**

### 12.2 原始数据（故意做脏）

| 成交日期 | 销售工号 | 产品 | 成交客户数 | 成交金额 | 业务组 |
| --- | --- | --- | --- | --- | --- |
| 2026/1/5 | 001 | 花呗 | 25 | 12500 | 广东-1组 |
| 2026/1/8 | 002 | 借呗 | 15 | 8900 | 广东-2组 |
| 2026/1/12 | 001 | 花呗 | 30 | 15600 | 广东-1组 |
| 2026/1/12 | 001 | 花呗 | 30 | 15600 | 广东-1组 |
|  |  |  |  |  |  |
| 2026/1/15 | 003 | 借呗 |   | 9200 | 广西-1组 |
| 2026.1.20 | 002 | 花呗 | 22 | 11800 | 广东-2组 |
| 2026/1/25 | 003 | 借呗 | 26 | 13500 | 广西-1组 |
| 2026/1/28 | 004 | 花呗 | 19 | 9800 | 海南-1组 |
| 2026/1/31 | 001 | 借呗 | 21 | 10800 | 广东-2组 |

**先自己找问题，别急着往下看**——这份数据里至少埋了 4 个坑。

### 12.3 任务：走完五步

**第 1 步 · 清洗**（第 0、2、3 章）

- 找出并列出所有脏数据问题
- 删除重复行、删除空行
- 统一日期格式
- 处理缺失的客户数
- `Ctrl+T` 转智能表

**第 2 步 · 建指标**（第 4、5、6 章）

- 加"成交月份"列
- 加"客单价"列（用 `IFERROR` 兜底）
- 加"业绩等级"列（≥15000 高、≥10000 中、其余低）
- 算出总额、平均、中位数、最高、最低

**第 3 步 · 分析**（第 7、8 章）

- 按业务组拆解，看各组贡献和占比
- 按产品拆解
- 找出帕累托的 80% 贡献项
- 找出表现最差的一笔，说明原因

**第 4 步 · 可视化**（第 10 章）

- 做一个包含核心指标卡 + 趋势/拆解图 + 明细表的看板
- 图表标题写**结论**而不是"趋势图"

**第 5 步 · 汇报**（第 10 章）

- 用 SCQA 结构写一段 200 字的结论
- 必须包含：现状、发现的问题、**可执行的建议**

### ✅ 参考结论（做完再看）

**清洗阶段发现的问题**

| 问题 | 位置 | 处理 |
| --- | --- | --- |
| 完全重复的记录 | 1/12 出现两次 | 删除重复值 |
| 空行 | 第 5 行 | 删除（会切断数据区） |
| 日期格式不统一 | `2026.1.20` 用点分隔 | 会被识别为文本，需转换成真日期，否则无法按月分组 |
| 客户数缺失 | 1/15 那一笔 | 金额不受影响，但**该笔客单价无法计算**——用 `IFERROR` 兜底 |

清洗后有效数据 **8 笔**，成交金额合计 **92100**。

> 🟡 **交叉验证**：这个 92100 应该等于本篇主数据里 1 月的总额。对得上，说明你洗对了。

**核心数字**

| 指标 | 值 |
| --- | --- |
| 成交总额 | 92100 |
| 笔数 | 8 |
| 平均单笔 | 11512.5 |
| 中位数 | 11300 |
| 最高 / 最低 | 15600 / 8900 |
| 花呗 / 借呗 | 49700（54.0%）/ 42400（46.0%） |

**拆解结论**

- **广东-2组 31500（34.2%）** 贡献最高，共 3 笔，是最稳的区域
- **广东-1组 28100（30.5%）** 排第二，但**只靠 2 笔大单撑着**——一旦其中一笔丢了，跌幅会很大，属于集中度风险
- **海南-1组只有 1 笔 9800**（10.6%），是明显的薄弱环节
- 帕累托：**前 2 个业务组贡献 64.7%**，前 3 个达 **89.4%**
- 金额最低的一笔是 1/8 的 8900，但它的客单价 593 其实不差——**金额小不等于做得差**，别混为一谈

**SCQA 汇报示例**

> **S**：1 月成交 92100，共 8 笔，平均单笔 11512.5，中位数 11300。
> **C**：区域发展严重不均——广东两个组合计占 **64.7%**，而海南-1组仅 1 笔（10.6%）；其中广东-1组的 28100 只由 2 笔大单构成，集中度风险高。
> **Q**：下个月资源该怎么分配？
> **A**：建议 ① 重点补强海南-1组（目前仅 1 笔，明显未打开市场）；② 关注广东-1组的客户集中度风险，推动其增加成交笔数以分散风险；③ 规范数据录入——本月数据存在重复行、空行、日期格式不统一、客户数缺失四类问题，建议改用下拉框和日期控件约束录入。

> 🟢 **注意最后那句**——分析师不只回答业务问题，还应该指出**数据本身的流程问题**。这是加分项。

### 🤖 问 AI

> "我清洗完一份销售数据后，得到了这些指标。请帮我检查：这些数字之间有没有矛盾？还有哪些角度是我漏掉没分析的？"

---

## 附录 A：报错排错速查

> 卡住时按错误提示反查。**学员卡住的时间，八成花在报错上。**

| 错误 | 含义 | 常见原因与解法 |
| --- | --- | --- |
| `#####` | 不是错误 | 列宽不够，拉宽即可 |
| `#DIV/0!` | 除以零 | 分母为 0 或空。用 `=IFERROR(公式,0)` 兜底 |
| `#N/A` | 找不到 | XLOOKUP/VLOOKUP 没匹配上。查**类型是否一致**（文本 vs 数字）、有无尾部空格（`TRIM`） |
| `#VALUE!` | 类型不对 | 拿文本做算术。检查文本型数字，用"分列"转换 |
| `#NAME?` | 名字不认识 | 函数名拼错，或文本参数漏了英文双引号（写了 `高` 而非 `"高"`） |
| `#REF!` | 引用失效 | 引用的单元格/区域被删了 |
| `#SPILL!` | 溢出被挡 | `FILTER`/`SORT` 的输出区域有内容挡住了，清空即可 |
| 弹出"公式有问题" | 语法错 | **几乎都是用了全角符号**：把 `≥` `≤` `≠` 改成 `>=` `<=` `<>`，引号改英文 |
| **结果不报错但不对** | 静默错误 | ① 筛选后用了 `SUM`（应改用 `SUBTOTAL`）；② 漏了绝对引用 `$`；③ 条件区域与求和区域行数不一致；④ 混了文本型数字；⑤ 源数据有空行/合并单元格 |

> 🔴 **最后一行最危险**。报错至少会提醒你，静默错误不会。

---

## 附录 B：函数速查

| 函数 | 用途 | 什么时候想到它 |
| --- | --- | --- |
| `SUM` / `AVERAGE` | 求和 / 平均 | 要一个汇总数 |
| `SUBTOTAL` | **筛选后**求和 | 边筛选边看数时（用 109） |
| `COUNT` / `COUNTA` | 数数值 / 数非空 | 要知道"有多少条" |
| `MAX` / `MIN` / `MEDIAN` | 最大 / 最小 / 中位数 | 看极值、看典型水平 |
| `QUARTILE` / `PERCENTILE` | 分位数 | 看分布、算 P90 |
| `RANK` / `LARGE` | 排名 / 第 N 大 | Top N 问题 |
| `ROUND` | 四舍五入 | 金额、比率定精度 |
| `IF` / `IFS` | 条件判断 | 打标签、分等级 |
| `IFERROR` | 错误兜底 | 做除法时一律包上 |
| `SUMIF` / `COUNTIF` | 单条件求和 / 计数 | "花呗卖了多少""有几笔过万" |
| `SUMIFS` / `COUNTIFS` | 多条件求和 / 计数 | 条件不止一个（**求和区在最前**） |
| `SUMPRODUCT` | 加权 / 跨列判断 | 需要更灵活的多条件计算 |
| `XLOOKUP` | 查找（推荐） | 按标识查信息，可反向、可容错 |
| `VLOOKUP` | 查找（旧） | 维护遗留表格时 |
| `FILTER` / `SORT` / `UNIQUE` | 动态数组 | 筛出一片、排序、去重清单 |
| `DATEDIF` / `EDATE` / `EOMONTH` | 日期间隔 / 偏移 / 月末 | 算账龄、算同期 |
| `YEAR` / `MONTH` / `TODAY` | 提取年 / 月 / 今天 | 按月汇总 |
| `LEFT` / `MID` / `RIGHT` | 截取 | 拆字符串 |
| `FIND` / `SEARCH` | 定位字符 | **让截取变动态** |
| `LEN` | 数字符数 | 校验数据位数 |
| `TEXT` / `SUBSTITUTE` / `TRIM` | 格式化 / 替换 / 去空格 | 清洗文本 |
| `&` / `TEXTJOIN` | 文本连接 | 拼接编号、批量合并 |

---

## 附录 C：快捷键

| 快捷键 | 作用 |
| --- | --- |
| `Ctrl+T` | 转智能表（拿到数据第一件事） |
| `Ctrl+E` | Flash Fill，智能提取/合并 |
| `Ctrl+S` / `F12` | 保存 / 另存为 |
| `Alt+=` | 自动求和 |
| `F4` | 切换引用方式（编辑公式时） |
| `Ctrl+Shift+L` | 加筛选 |
| `Ctrl+F` / `Ctrl+H` | 查找 / 替换 |
| `Ctrl+;` | 输入当天日期 |
| `Alt+F5` | 刷新透视表 / 查询 |
| `F2` | 进入单元格编辑 |

---

## 最后：给你的三条建议

1. **优先掌握三样**：`Ctrl+T`、数据透视表、`XLOOKUP`。这三样投入产出比最高，能解决日常工作八成的问题。

2. **做完改一次数据验证联动**：改一个源数字，看结果是否跟着变。**这是检验"真学会"的唯一标准**。

3. **练"拆解"而不是背函数**：函数记不住可以问 AI，但"看到下滑就想到按维度拆"这个反应，没人能替你建立。

> 🟢 AI 时代，记语法的价值在下降，**判断力和提问能力在上升**。把时间花在第 8、10 章，比背 50 个函数划算得多。
