# 参与改进

欢迎反馈。这份教程最怕的不是被挑错，而是**错着没人说**——尤其是数字错误。

## 发现数字不对？请直接开 Issue

教程里所有参考答案都必须用公式算出来。如果你算出来的和文档写的不一样，**大概率是文档错了**。

开 Issue 时请带上这四样，复核会很快：

1. 哪个文件、第几章（如 `v3.0 第 8.5 节 帕累托`）
2. 文档写的数字
3. 你算出来的数字
4. 你用的公式，或你的计算过程

> 举例：`v3.0 第 6 章 Q1 写的是 9300，我用 =QUARTILE(E2:E25,1) 算出 9500。`
> —— 这类反馈曾真实修掉过一处错误（INC / EXC 口径混用），见 [CHANGELOG](CHANGELOG.md)。

## 提 PR

改动教程正文时，请遵守这些约定：

### 1. 答案必须用公式算，不能手算

新增或修改任何 `✅ 参考答案` 里的数字，都要在 Excel 里用文档给出的公式实际跑一遍，并做**三项校验**
（见 v3.0 第 10.5 节）：

- **合计校验**：透视表合计是否等于 `SUM`
- **交叉校验**：换一种方法再算一遍（SUMIFS 的结果对一遍透视表）
- **抽样核对**：挑一条记录手工验一遍

改动数字时，请在 PR 描述里写明校验方式。

### 2. 注意函数口径

`QUARTILE` / `PERCENTILE` 默认是 INC，`QUARTILE.EXC` / `PERCENTILE.EXC` 是另一套算法，
**结果不同但都不算错**。文档统一用默认的 INC；如果确需用 EXC，必须在正文标明。

### 3. 保持既有结构

每章的骨架是：`练一练` → `✅ 参考答案（做完再看）` → `🤖 问 AI`。新增内容请沿用。

提示标记的含义：

- 🔴 容易踩的坑 / 会造成静默错误
- 🟡 补充说明或观察
- 🟢 原则、经验、值得记住的
- 🔵 章节定位说明

### 4. 数据集不要随便动

`practice-data.xlsx` 的 `24行数据` 是 v3.0 全篇的主数据集，`10行数据` 是 v2.0 的。
改动其中任何一个数字，会导致**全篇参考答案失效**。确需更换数据集，请在 Issue 里先讨论。

## 想加截图或示例

目前教程是纯文字版，没有操作截图。如果你愿意补充（尤其是 Power Query、Power Pivot 这类界面操作密集的章节），
非常欢迎——这是目前最主要的短板。图片请放在 `assets/` 下，用相对路径引用。

## 许可

你的贡献将同样以 [CC BY-NC-SA 4.0](LICENSE.md) 发布。提交 PR 即表示你同意按此协议授权。
